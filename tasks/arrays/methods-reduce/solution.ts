export interface Item {
  name: string;
  price: number;
}

/**
 * Напишите функцию sumPrices(items), которая принимает массив объектов
 * типа Item и возвращает общую сумму цен всех товаров.
 * 
 * Важно: вы должны обязательно использовать метод массива reduce().
 * Если список пуст, верните 0.
 * 
 * @param items Массив объектов товаров
 * @returns Суммарная цена всех товаров
 */
export function sumPrices(items: Item[]): number {
  // Напишите ваш код здесь
  return 0;
}
