/**
 * Напишите функцию findMax(arr), которая принимает массив чисел
 * и возвращает максимальное число в массиве.
 * Если массив пустой, функция должна вернуть undefined.
 * 
 * Важно: не используйте встроенный метод Math.max().
 * 
 * @param arr Массив чисел
 * @returns Максимальное число или undefined
 */
export function findMax(arr: number[]): number | undefined {
  // Напишите ваш код здесь
  return undefined;
}
