/**
 * Реализуйте функцию bubbleSort(arr), которая сортирует массив чисел
 * по возрастанию НА МЕСТЕ с помощью алгоритма пузырьковой сортировки.
 * 
 * Требования:
 * 1. Сортировка должна выполняться in-place (мутировать переданный массив).
 * 2. Используйте флаг досрочного выхода swapped (если за целый проход
 *    не было ни одного обмена, массив уже отсортирован).
 * 
 * @param arr Массив чисел для сортировки
 * @returns Отсортированный массив (тот же самый объект в памяти)
 */
export function bubbleSort(arr: number[]): number[] {
  // Напишите ваш код здесь
  return arr;
}
