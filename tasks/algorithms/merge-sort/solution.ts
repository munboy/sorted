/**
 * Реализуйте функцию mergeSort(arr), которая сортирует массив чисел
 * по возрастанию с помощью алгоритма сортировки слиянием.
 * 
 * Требования:
 * 1. Временная сложность должна составлять строго O(n log n).
 * 2. Допускается выделение дополнительной памяти под буферы слияния (пространственная сложность O(n)).
 * 3. Алгоритм должен сохранять стабильность сортировки.
 * 
 * @param arr Массив чисел
 * @returns Новый или измененный отсортированный массив
 */
export function mergeSort(arr: number[]): number[] {
  // Напишите ваш код здесь
  return arr;
}
