/**
 * Реализуйте функцию insertionSort(arr), которая сортирует массив чисел
 * по возрастанию НА МЕСТЕ с помощью алгоритма сортировки вставками.
 * 
 * Требования:
 * 1. Сортировка должна выполняться in-place (мутировать исходный массив).
 * 2. Сдвигайте элементы вправо через присвоение arr[j + 1] = arr[j],
 *    избегая медленной деструктуризации swap на каждом шаге сдвига.
 * 
 * @param arr Массив чисел
 * @returns Отсортированный массив (тот же самый объект в памяти)
 */
export function insertionSort(arr: number[]): number[] {
  // Напишите ваш код здесь
  return arr;
}
