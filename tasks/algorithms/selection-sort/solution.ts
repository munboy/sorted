/**
 * Реализуйте функцию selectionSort(arr), которая сортирует массив чисел
 * по возрастанию НА МЕСТЕ с помощью алгоритма сортировки выбором.
 * 
 * Требования:
 * 1. Сортировка должна выполняться in-place (мутировать массив).
 * 2. Минимизируйте количество обменов (свопов) — обмен должен происходить
 *    максимум 1 раз за каждый внешний проход цикла.
 * 
 * @param arr Массив чисел
 * @returns Отсортированный массив (тот же самый объект в памяти)
 */
export function selectionSort(arr: number[]): number[] {
  // Напишите ваш код здесь
  return arr;
}
