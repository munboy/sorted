/**
 * Реализуйте функцию heapSort(arr), которая сортирует массив чисел
 * по возрастанию НА МЕСТЕ с помощью алгоритма пирамидальной сортировки (Max-Heap).
 * 
 * Требования:
 * 1. Сортировка должна выполняться in-place (без создания вспомогательных массивов).
 * 2. Пространственная сложность должна составлять строго O(1) по памяти.
 * 3. Временная сложность должна составлять строго O(n log n).
 * 
 * @param arr Массив чисел
 * @returns Отсортированный массив (тот же самый объект в памяти)
 */
export function heapSort(arr: number[]): number[] {
  // Напишите ваш код здесь
  return arr;
}
