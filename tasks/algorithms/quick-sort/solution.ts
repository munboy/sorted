/**
 * Реализуйте функцию quickSort(arr, left, right), которая сортирует массив чисел
 * по возрастанию НА МЕСТЕ с помощью алгоритма быстрой сортировки (схема Ломуто).
 * 
 * Требования:
 * 1. Алгоритм должен выполняться строго in-place (без создания временных массивов-буферов).
 * 2. Функция должна принимать необязательные аргументы left и right.
 *    По умолчанию left = 0, right = arr.length - 1.
 * 
 * @param arr Массив чисел
 * @param left Левая граница сортировки
 * @param right Правая граница сортировки
 * @returns Отсортированный массив (тот же самый объект в памяти)
 */
export function quickSort(
  arr: number[],
  left: number = 0,
  right: number = arr.length - 1
): number[] {
  // Напишите ваш код здесь
  return arr;
}
