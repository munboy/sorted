/**
 * Напишите функцию sortNumbers(arr), которая принимает массив чисел,
 * сортирует его по возрастанию НА МЕСТЕ и возвращает тот же самый отсортированный массив.
 * 
 * Важно: убедитесь, что функция правильно работает с числами разной длины
 * (например, 10, 5, 2, 20, 1), защитившись от дефолтного лексикографического поведения sort().
 * 
 * @param arr Массив чисел
 * @returns Отсортированный массив (тот же самый объект в памяти)
 */
export function sortNumbers(arr: number[]): number[] {
  // Напишите ваш код здесь
  return arr;
}
