/**
 * Напишите функцию immutableSort(arr), которая принимает массив чисел
 * и возвращает новый массив, отсортированный по убыванию.
 * 
 * Важно: функция НЕ должна изменять (мутировать) исходный массив.
 * Используйте метод toSorted() или предварительное копирование массива.
 * 
 * @param arr Исходный массив чисел
 * @returns Новый отсортированный по убыванию массив
 */
export function immutableSort(arr: number[]): number[] {
  // Напишите ваш код здесь
  return [];
}
