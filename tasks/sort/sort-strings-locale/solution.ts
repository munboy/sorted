/**
 * Напишите функцию safeSortStrings(arr), которая сортирует массив строк по возрастанию
 * (в алфавитном порядке) с использованием метода localeCompare с русской локалью.
 * 
 * Требования:
 * 1. Массив может содержать невалидные значения: null, undefined и пустые строки "".
 * 2. Вы должны безопасно переместить все эти невалидные/пустые значения в самый конец
 *    отсортированного массива.
 * 3. Функция должна изменять исходный массив на месте и возвращать его.
 * 
 * @param arr Массив строк, который может содержать null, undefined или ""
 * @returns Отсортированный массив (тот же самый объект в памяти)
 */
export function safeSortStrings(
  arr: (string | null | undefined)[]
): (string | null | undefined)[] {
  // Напишите ваш код здесь
  return arr;
}
