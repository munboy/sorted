export interface User {
  name: string;
  age: number;
}

/**
 * Напишите функцию sortUsers(users), которая принимает массив объектов
 * типа User и сортирует его по имени (в алфавитном порядке),
 * а при совпадении имен — по возрасту (по возрастанию).
 * Сортировка должна осуществляться НА МЕСТЕ и возвращать тот же самый массив.
 * 
 * Важно: используйте метод localeCompare() для корректного сравнения кириллических имен.
 * 
 * @param users Массив пользователей
 * @returns Отсортированный массив пользователей (тот же самый объект в памяти)
 */
export function sortUsers(users: User[]): User[] {
  // Напишите ваш код здесь
  return users;
}
