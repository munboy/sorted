export type BlockType = 'paragraph' | 'code' | 'list' | 'alert' | 'heading';

export interface TheoryBlock {
  type: BlockType;
  level?: 1 | 2 | 3;
  alertType?: 'info' | 'warning' | 'error';
  language?: string;
  content: string | string[]; // string for text/code, string[] for list items
}

export interface ExampleCode {
  title: string;
  code: string;
  explanation: string;
}

export interface InterviewQA {
  question: string;
  interviewerExpectation: string;
  verbalStructure: string;
  firstMention: string;
  commonAnswerMistakes: string;
  strongAnswer: string;
  followUpQuestions: string[];
}

export interface QuizQuestion {
  id: string;
  question: string;
  options: string[];
  correctAnswerIndex: number;
  explanation: string;
}

export interface Lesson {
  slug: string;
  title: string;
  section: 'fundamentals' | 'algorithms' | 'advanced' | 'production';
  sectionTitle: string;
  order: number;
  difficulty: 'Легкий' | 'Средний' | 'Сложный';
  estimatedMinutes: number;
  prerequisites: string[]; // slugs
  summary: string;
  whatToKnowBefore: string;
  keyTerms: { term: string; definition: string }[];
  theory: TheoryBlock[];
  stepByStep: TheoryBlock[];
  whenToUse: string;
  whenNotToUse: string;
  rightExample: ExampleCode;
  wrongExample: ExampleCode;
  commonMistakes: string[];
  checklist: string[];
  interviewQA: InterviewQA[];
  relatedTopics: string[]; // slugs
  quizzes: QuizQuestion[];
  tasks: string[]; // task slugs
}

export const lessons: Lesson[] = [
  {
    slug: 'arrays-iteration',
    title: 'Массивы и основы итерации',
    section: 'fundamentals',
    sectionTitle: 'Базовые знания перед сортировкой',
    order: 1,
    difficulty: 'Легкий',
    estimatedMinutes: 15,
    prerequisites: [],
    summary: 'Изучение структуры массивов в JavaScript, работа с индексами, длиной массива и базовыми циклами для перебора элементов.',
    whatToKnowBefore: 'Базовый синтаксис JavaScript (переменные, типы данных, условные операторы).',
    keyTerms: [
      { term: 'Массив (Array)', definition: 'Упорядоченная структура данных для хранения последовательности значений. В JS массив — это объект с числовыми ключами-индексами.' },
      { term: 'Индекс (Index)', definition: 'Числовой номер позиции элемента в массиве. Всегда начинается с 0, то есть первый элемент имеет индекс 0, второй — 1, и так далее.' },
      { term: 'Длина (length)', definition: 'Свойство массива, равное максимальному числовому индексу плюс единица. НЕ просто количество элементов — важно понимать разницу при работе с разреженными массивами.' },
      { term: 'Разреженный массив (Sparse Array)', definition: 'Массив, в котором некоторые индексы не содержат значений — в них образуются «дыры» (empty slots). Они отличаются от undefined: map/forEach пропускают дыры, но не пропускают undefined.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Массивы в JavaScript — это не просто список ячеек в памяти, как в C или Java. Под капотом массив JS — это обычный объект, где числовые индексы являются строковыми ключами свойств. Поэтому поведение многих операций с массивами может вас удивить, если воспринимать их как «настоящие» низкоуровневые массивы.' },
      { type: 'heading', level: 2, content: 'Как работает свойство length — главная ловушка' },
      { type: 'paragraph', content: 'Самое важное: length НЕ равно «количество элементов». Он всегда равен (максимальный индекс + 1). Посмотрите на этот код — это именно то, что стоит за вопросом из квиза:' },
      { type: 'code', language: 'javascript', content: `const a = [1, 2];     // Элементы на индексах 0 и 1
a.length;              // → 2 (max индекс = 1, поэтому 1 + 1 = 2)

// Теперь делаем прыжок через индексы:
a[5] = 5;             // Присваиваем значение на индекс 5

// Что стало с массивом?
a.length;              // → 6 (max индекс теперь = 5, поэтому 5 + 1 = 6)

// А что на позициях 2, 3, 4?
a[2];                  // → undefined (пустая ячейка, "дыра")
a[3];                  // → undefined
a[4];                  // → undefined

console.log(a);        // → [1, 2, empty × 3, 5]
                       // Три позиции — просто дыры, не undefined-значения!` },
      { type: 'alert', alertType: 'info', content: 'Разница между «дырой» и undefined: если вы сделаете a[2] = undefined явно — это будет настоящий undefined. А если индекс просто пропущен — это «дыра». Методы map, forEach, filter пропустят дыры, но обработают явный undefined. Это тонкость, которую легко упустить.' },
      { type: 'heading', level: 2, content: 'Что происходит при ручном изменении length' },
      { type: 'paragraph', content: 'Length — это обычное перезаписываемое свойство. Если его уменьшить, элементы в хвосте массива будут удалены навсегда. Если увеличить — появятся новые дыры:' },
      { type: 'code', language: 'javascript', content: `const arr = [1, 2, 3, 4, 5];

// Уменьшаем length — элементы в хвосте безвозвратно удаляются
arr.length = 3;
console.log(arr); // → [1, 2, 3]

// Увеличиваем length — появляются дыры
arr.length = 6;
console.log(arr); // → [1, 2, 3, empty × 3]

// Хак для очистки массива (мутирует исходный, работает быстро)
arr.length = 0;
console.log(arr); // → []` },
      { type: 'heading', level: 2, content: 'Три способа перебрать массив — и когда какой выбрать' },
      { type: 'paragraph', content: 'Классический for-цикл даёт полный контроль: можно двигаться в любом направлении, менять шаг, выходить по break. Именно его используют во всех алгоритмах сортировки. Цикл for...of прост и читаем, но нет доступа к индексу. Цикл for...in предназначен для объектов — в массивах он опасен, объяснение ниже.' },
      { type: 'code', language: 'javascript', content: `const fruits = ['яблоко', 'банан', 'вишня'];

// 1. Классический for — индекс доступен, можно идти назад, можно прыгать
for (let i = 0; i < fruits.length; i++) {
  console.log(i, fruits[i]); // 0 'яблоко', 1 'банан', 2 'вишня'
}

// 2. for...of — читабельно, но нет индекса
for (const fruit of fruits) {
  console.log(fruit); // 'яблоко', 'банан', 'вишня'
}

// 3. for...in — НЕ ИСПОЛЬЗУЙТЕ для массивов!
// Он обходит ВСЕ перечисляемые свойства, включая нестандартные
fruits.myProp = 'ой';
for (const key in fruits) {
  console.log(key); // '0', '1', '2', 'myProp' — сюрприз!
}` },
      { type: 'alert', alertType: 'warning', content: 'Никогда не используйте for...in для массивов. Во-первых, он возвращает строковые ключи («0», «1»), а не числа. Во-вторых, он обходит унаследованные свойства прототипа. В-третьих, порядок обхода не гарантирован спецификацией (хотя на практике V8 его соблюдает).' },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Как писать вложенный цикл для попарного сравнения элементов' },
      { type: 'paragraph', content: 'Во всех алгоритмах сортировки нам нужно сравнивать пары элементов. Посмотрим как правильно строить вложенный цикл и почему внутренний начинается с i + 1:' },
      { type: 'code', language: 'javascript', content: `const arr = [3, 1, 2];

// Внешний цикл: выбирает «первый» элемент пары
for (let i = 0; i < arr.length - 1; i++) {
  // Внутренний цикл: сравниваем с КАЖДЫМ элементом ПРАВЕЕ i
  // Начинаем с i + 1, а не с 0 — иначе будем сравнивать (0,0), (1,0) и т.д.
  // то есть элемент сам с собой и повторные пары
  for (let j = i + 1; j < arr.length; j++) {
    console.log(\`Сравниваем arr[\${i}]=\${arr[i]} и arr[\${j}]=\${arr[j]}\`);
  }
}
// Выведет:
// Сравниваем arr[0]=3 и arr[1]=1
// Сравниваем arr[0]=3 и arr[2]=2
// Сравниваем arr[1]=1 и arr[2]=2
// Итого 3 пары (N*(N-1)/2) — каждая пара ровно по одному разу` },
      { type: 'list', content: [
        'Внешний цикл i идёт до length - 1 (не до length), потому что для последнего элемента нет правого соседа.',
        'Внутренний цикл j начинается с i + 1, а не с 0. Это исключает повторные пары: пара (0,1) и (1,0) — это одна и та же пара, нам не нужна обе.',
        'В теле внутреннего цикла обращаемся к arr[i] и arr[j]. Если хотим поменять их местами — используем деструктуризацию: [arr[i], arr[j]] = [arr[j], arr[i]].'
      ]}
    ],
    whenToUse: 'Используйте классический цикл for, когда вам нужно изменять элементы на месте, перебирать массив в обратном порядке, или совершать шаг отличный от 1. Используйте for...of для быстрого чтения элементов без изменений.',
    whenNotToUse: 'Не используйте for...in для перебора массивов — он обходит все перечисляемые свойства объекта, включая нечисловые ключи и свойства из цепочки прототипов.',
    rightExample: {
      title: 'Правильный обход массива — классический for',
      code: `const prices = [100, 250, 400];
let total = 0;
for (let i = 0; i < prices.length; i++) {
  total += prices[i];
}
console.log(total); // 750

// Обратный обход — просто меняем направление
for (let i = prices.length - 1; i >= 0; i--) {
  console.log(prices[i]); // 400, 250, 100
}`,
      explanation: 'Строгое неравенство i < prices.length гарантирует работу в пределах валидных индексов 0, 1 и 2. Чтение prices[prices.length] вернёт undefined и испортит подсчёт суммы.'
    },
    wrongExample: {
      title: 'Ошибка: <= вместо < — выход за границы массива',
      code: `const prices = [100, 250, 400];
let total = 0;
// Ошибка: <= вместо <
for (let i = 0; i <= prices.length; i++) {
  total += prices[i];
}
console.log(total); // NaN

// Почему NaN? На последней итерации i = 3.
// prices[3] не существует → возвращает undefined
// 750 + undefined = NaN (любая арифметика с undefined даёт NaN)`,
      explanation: 'Условие i <= prices.length приводит к тому, что на последней итерации i будет равно 3. Элемента prices[3] не существует — JS вернёт undefined, а любое число плюс undefined равно NaN.'
    },
    commonMistakes: [
      'Использование <= вместо < в условии for-цикла — приводит к обращению за границы массива (значение undefined вместо реального элемента).',
      'Попытка изменить элемент через for...of: `for (const item of arr) { item = 0; }` — item это копия, исходный массив не изменится. Для изменений нужен индекс.',
      'Использование for...in для массивов — возвращает строковые ключи и обходит все перечисляемые свойства, включая кастомные.'
    ],
    checklist: [
      'Знать, что length = (максимальный индекс + 1), а не просто количество заполненных элементов.',
      'Понимать, что такое разреженный массив и как образуются «дыры».',
      'Уметь обходить одномерный массив классическим циклом в обоих направлениях.',
      'Уметь писать вложенные циклы для попарного сравнения элементов (с j = i + 1).'
    ],
    interviewQA: [
      {
        question: 'Что вернёт код: const a = [1, 2]; a[5] = 5; console.log(a.length)?',
        interviewerExpectation: 'Интервьюер проверяет понимание того, что length = maxIndex + 1, а не количество реальных значений, и знание разреженных массивов.',
        verbalStructure: 'Назовите ответ (6), объясните почему (maxIndex = 5, length = 5 + 1), упомяните что индексы 2, 3, 4 стали дырами (empty), а не undefined.',
        firstMention: 'Ответ: 6. Length всегда равен (максимальный числовой индекс + 1). Мы присвоили значение на индекс 5, значит length стал 5 + 1 = 6.',
        commonAnswerMistakes: 'Ответ «3», «5» или «undefined». Или ответ «6» без объяснения механизма — интервьюер захочет услышать про maxIndex + 1.',
        strongAnswer: 'В JavaScript массив — это объект, числовые индексы — это строковые ключи его свойств. Свойство length всегда поддерживается равным (максимальный числовой ключ + 1). После `a[5] = 5` максимальный ключ стал 5, поэтому length = 6. Индексы 2, 3, 4 не были явно заданы — они стали «дырами» (empty slots). Обращение к ним вернёт undefined, но методы map/forEach пропустят их, в отличие от явно записанного undefined.',
        followUpQuestions: [
          'Чем отличается дыра (empty slot) от явно записанного undefined?',
          'Что произойдёт если сделать a.length = 2 после всех этих операций?'
        ]
      }
    ],
    relatedTopics: ['array-methods', 'complexity-basics'],
    quizzes: [
      {
        id: 'quiz-arrays-1',
        question: 'Что выведет код: const a = [1, 2]; a[5] = 5; console.log(a.length);?',
        options: ['2', '6', '5', 'undefined'],
        correctAnswerIndex: 1,
        explanation: 'length = максимальный индекс + 1. Мы записали элемент на индекс 5, значит length = 5 + 1 = 6. Индексы 2, 3, 4 стали пустыми ячейками («дырами»).'
      }
    ],
    tasks: ['arrays-basic-iteration']
  },
  {
    slug: 'array-methods',
    title: 'Методы массивов в деталях',
    section: 'fundamentals',
    sectionTitle: 'Базовые знания перед сортировкой',
    order: 2,
    difficulty: 'Легкий',
    estimatedMinutes: 20,
    prerequisites: ['arrays-iteration'],
    summary: 'Анализ работы встроенных методов добавления/удаления, вырезания и трансформации массивов. Разделение на мутирующие и немутирующие методы.',
    whatToKnowBefore: 'Уверенное понимание индексов массивов и циклов.',
    keyTerms: [
      { term: 'Мутация (Mutation)', definition: 'Изменение исходной структуры данных на месте (in-place) без создания нового объекта. После мутации исходная переменная указывает на изменённые данные.' },
      { term: 'Иммутабельность (Immutability)', definition: 'Принцип, при котором оригинальные данные никогда не изменяются — вместо этого создаётся новая копия с изменениями. Критично для React и Redux.' },
      { term: 'Чистая функция (Pure Function)', definition: 'Функция без побочных эффектов: не изменяет внешние переменные и возвращает новый результат на основе входных данных. При одинаковых входных данных всегда возвращает одно и то же.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Методы массивов в JavaScript делятся на две категории: мутирующие (изменяют оригинал) и немутирующие (возвращают новый массив, не трогая исходный). Это разделение — источник огромного числа труднонаходимых багов, особенно в React-приложениях.' },
      { type: 'heading', level: 2, content: 'Мутирующие методы — изменяют оригинал' },
      { type: 'code', language: 'javascript', content: `const arr = [1, 2, 3];

// push/pop — работают с КОНЦОМ массива, O(1)
arr.push(4);        // → arr = [1, 2, 3, 4], возвращает новую длину (4)
arr.pop();          // → arr = [1, 2, 3], возвращает удалённый элемент (4)

// unshift/shift — работают с НАЧАЛОМ массива, O(n) — медленно!
// Потому что все элементы сдвигаются на одну позицию
arr.unshift(0);     // → arr = [0, 1, 2, 3], возвращает новую длину (4)
arr.shift();        // → arr = [1, 2, 3], возвращает удалённый элемент (0)

// splice — самый мощный мутирующий метод
// splice(startIndex, deleteCount, ...itemsToInsert)
arr.splice(1, 1);         // Удаляет 1 элемент начиная с индекса 1
                          // → arr = [1, 3], возвращает удалённые: [2]
arr.splice(1, 0, 'x');    // Вставляет 'x' на индекс 1, ничего не удаляя
                          // → arr = [1, 'x', 3]

// reverse и sort — тоже мутирующие!
arr.reverse();      // Переворачивает на месте, возвращает ту же ссылку` },
      { type: 'heading', level: 2, content: 'Немутирующие методы — возвращают новый массив' },
      { type: 'code', language: 'javascript', content: `const original = [1, 2, 3, 4, 5];

// slice — вырезает часть без изменения оригинала
// slice(startIndex, endIndex) — endIndex НЕ включается
const part = original.slice(1, 3);  // → [2, 3]
console.log(original);              // → [1, 2, 3, 4, 5] (не изменился!)

// Трюк: slice() без аргументов делает полную копию массива
const copy = original.slice();      // → [1, 2, 3, 4, 5], новый объект
console.log(original === copy);     // → false (разные объекты в памяти)

// Или через spread-оператор (современный способ):
const copy2 = [...original];

// map — трансформирует каждый элемент, возвращает новый массив той же длины
const doubled = original.map(n => n * 2);  // → [2, 4, 6, 8, 10]

// filter — отбирает элементы, возвращает новый массив (возможно короче)
const evens = original.filter(n => n % 2 === 0);  // → [2, 4]

// concat — объединяет массивы без мутации
const joined = original.concat([6, 7]);  // → [1, 2, 3, 4, 5, 6, 7]` },
      { type: 'alert', alertType: 'warning', content: 'Запомните: slice не трогает оригинал, splice — мутирует! Их названия похожи, это один из самых частых источников ошибок. Разница: slice = «нарезать» (новая копия), splice = «сшивать/разрезать» (изменяет оригинал).' },
      { type: 'heading', level: 2, content: 'Почему мутация опасна в React' },
      { type: 'code', language: 'javascript', content: `// ПЛОХО: мутируем массив состояния в React
const [items, setItems] = useState([1, 2, 3]);

function badSort() {
  items.sort();        // Мутируем! Ссылка items не меняется
  setItems(items);     // React видит ту же ссылку → решает НЕ перерендеривать
  // Баг: данные изменились, но UI не обновился!
}

// ХОРОШО: создаём новый массив
function goodSort() {
  const sorted = [...items].sort(); // Копируем, затем сортируем копию
  setItems(sorted);   // Новая ссылка → React перерендерит компонент
}` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Почему unshift медленнее push — механика сдвига' },
      { type: 'list', content: [
        'push(item): добавляет элемент в конец. JS просто записывает значение на индекс arr.length и увеличивает length. Никаких сдвигов — O(1).',
        'unshift(item): добавляет в начало. JS должен сдвинуть КАЖДЫЙ существующий элемент вправо на 1 позицию: индекс 0 → 1, 1 → 2, и так для всех N элементов. Это O(n).',
        'Пример: массив из 1 000 000 элементов. push выполнится мгновенно. unshift сделает 1 000 000 операций перезаписи — это заметная задержка.',
        'Правило: если нужно часто добавлять в начало — используйте другую структуру данных (Linked List) или переверните логику (добавляйте в конец, читайте с конца).'
      ]}
    ],
    whenToUse: 'Используйте slice для создания копий перед сортировкой. Используйте map/filter/reduce для функциональных цепочек обработки данных без изменения оригинала.',
    whenNotToUse: 'Не используйте splice, если вам нужно сохранить исходный массив. Не используйте map, если вы не используете возвращаемый массив (пустая трата памяти — используйте forEach или классический цикл).',
    rightExample: {
      title: 'Безопасное удаление элемента из массива (иммутабельно)',
      code: `const users = ['Иван', 'Анна', 'Петр'];

// filter создаёт НОВЫЙ массив, исключая ненужный элемент
const updatedUsers = users.filter(user => user !== 'Анна');

console.log(users);        // ['Иван', 'Анна', 'Петр'] (оригинал цел!)
console.log(updatedUsers); // ['Иван', 'Петр'] (новый объект)
console.log(users === updatedUsers); // false`,
      explanation: 'filter — немутирующий метод. Он создаёт новый массив с элементами, удовлетворяющими условию. Исходный массив users не изменился ни на байт.'
    },
    wrongExample: {
      title: 'Непреднамеренная мутация через splice',
      code: `const users = ['Иван', 'Анна', 'Петр'];

// splice мутирует users!
const removed = users.splice(1, 1);

console.log(users);   // ['Иван', 'Петр'] ← ИЗМЕНИЛСЯ!
console.log(removed); // ['Анна'] ← удалённый элемент

// В React-компоненте это приведёт к скрытому багу:
// состояние мутировано, но React может не заметить изменение ссылки`,
      explanation: 'splice изменил исходный массив users. Если этот массив был передан как пропс в другой компонент или хранится в state — это баг, который очень трудно найти.'
    },
    commonMistakes: [
      'Путаница slice и splice: оба «режут» массив, но slice не трогает оригинал, а splice мутирует его.',
      'Не возвращать значение из колбэка map или filter: `arr.map(item => { item * 2 })` — функция возвращает undefined, получится массив из undefined.',
      'Использовать map вместо forEach когда возвращаемый массив не нужен — это создаёт мусорный массив и нагружает Garbage Collector.'
    ],
    checklist: [
      'Чётко различать мутирующие (push, pop, shift, unshift, splice, sort, reverse) и немутирующие (slice, map, filter, concat, spread) методы.',
      'Понимать разницу в производительности: push/pop = O(1), shift/unshift = O(n).',
      'Уметь безопасно копировать массив через slice() или [...arr] перед изменениями.'
    ],
    interviewQA: [
      {
        question: 'В чём разница между методами slice и splice? Какой из них мутирует массив?',
        interviewerExpectation: 'Интервьюер хочет не только разницу в синтаксисе, но и понимание концепции иммутабельности и практических последствий.',
        verbalStructure: '1. Назовите главное отличие (мутация vs копия). 2. Объясните аргументы каждого. 3. Дайте конкретный пример где мутация приводит к багу (React state).',
        firstMention: 'Главное отличие: slice возвращает новый массив и не изменяет оригинал; splice изменяет исходный массив на месте.',
        commonAnswerMistakes: 'Путаница в аргументах: утверждение что slice принимает количество удаляемых элементов (нет — это splice). Или ответ только про синтаксис без упоминания мутации.',
        strongAnswer: 'slice(start, end) — немутирующий: возвращает копию части массива от индекса start до end (не включая end). Оригинал не трогает. splice(start, deleteCount, ...items) — мутирующий: удаляет deleteCount элементов начиная с start, вставляет items вместо них, возвращает массив удалённых. На практике: в React нельзя вызывать splice на массиве состояния, потому что ссылка на объект не изменится и компонент не перерисуется.',
        followUpQuestions: [
          'Как сделать полную копию массива через slice?',
          'Какова временная сложность вставки через splice в начало массива?'
        ]
      }
    ],
    relatedTopics: ['stability-immutability', 'sort-comparators'],
    quizzes: [
      {
        id: 'quiz-array-methods-1',
        question: 'Какой из следующих методов НЕ мутирует исходный массив?',
        options: ['pop()', 'unshift()', 'concat()', 'reverse()'],
        correctAnswerIndex: 2,
        explanation: 'concat() создаёт новый объединённый массив, не трогая оригинал. pop(), unshift() и reverse() изменяют исходный массив на месте.'
      }
    ],
    tasks: ['array-methods-reduce']
  },
  {
    slug: 'sort-comparators',
    title: 'Array.prototype.sort и компараторы',
    section: 'fundamentals',
    sectionTitle: 'Базовые знания перед сортировкой',
    order: 3,
    difficulty: 'Средний',
    estimatedMinutes: 25,
    prerequisites: ['array-methods'],
    summary: 'Разбор встроенного метода сортировки, принципы работы функции сравнения (компаратора), сортировка чисел, строк и сложных объектов.',
    whatToKnowBefore: 'Понимание возвращаемых значений функций-колбэков, сравнение примитивных типов данных.',
    keyTerms: [
      { term: 'Компаратор (Comparator)', definition: 'Функция-колбэк для sort(), принимающая два элемента (a, b) и возвращающая число: отрицательное (a перед b), положительное (b перед a), 0 (порядок не важен).' },
      { term: 'Лексикографический порядок', definition: 'Сортировка по символам слева направо, как в словаре. «10» идёт перед «9», потому что символ «1» < «9». Именно это делает sort() без компаратора с числами.' },
      { term: 'localeCompare', definition: 'Метод строки для правильного сравнения текста с учётом языка (кириллица, диакритика, регистр). Использует правила конкретной локали.' }
    ],
    theory: [
      { type: 'paragraph', content: 'По умолчанию Array.prototype.sort() сортирует всё как строки. Это значит, что число 10 превращается в строку «10», и сравнивается посимвольно: «1» < «9», поэтому «10» окажется перед «9». Это поведение задано стандартом ECMA-262 — не баг, а осознанное решение для универсальности. Для правильной сортировки чисел нужен компаратор.' },
      { type: 'code', language: 'javascript', content: `// БЕЗ компаратора — сортирует как строки, результат неожиданный
const nums = [10, 5, 2, 20, 1];
nums.sort();
console.log(nums); // [1, 10, 2, 20, 5]
// Почему? '1' < '10' < '2' < '20' < '5' — в алфавитном порядке!

// С компаратором (a - b) — математическая сортировка по возрастанию
const nums2 = [10, 5, 2, 20, 1];
nums2.sort((a, b) => a - b);
console.log(nums2); // [1, 2, 5, 10, 20] — правильно!

// По убыванию — просто меняем порядок
nums2.sort((a, b) => b - a);
console.log(nums2); // [20, 10, 5, 2, 1]` },
      { type: 'heading', level: 2, content: 'Как работает компаратор — три правила' },
      { type: 'paragraph', content: 'Компаратор — это соглашение между вами и алгоритмом сортировки. Вы возвращаете число, алгоритм решает куда поставить элементы:' },
      { type: 'code', language: 'javascript', content: `// Компаратор (a, b) => результат:
//   результат < 0  →  a идёт ПЕРЕД b
//   результат > 0  →  b идёт ПЕРЕД a (a уходит вправо)
//   результат = 0  →  порядок не меняется

// Разберём (a, b) => a - b на примере:
// a = 15, b = 8:  15 - 8 = 7 > 0  →  b(8) перед a(15)  → верно!
// a = 3,  b = 8:  3 - 8 = -5 < 0  →  a(3) перед b(8)   → верно!
// a = 5,  b = 5:  5 - 5 = 0       →  порядок не важен  → верно!

// ОШИБКА НОВИЧКА: возвращать булево значение
[3, 1, 2].sort((a, b) => a > b);  // НЕПРАВИЛЬНО! true/false приводится к 1/0
// Это работает иногда, но не гарантирует корректную сортировку во всех движках
// Компаратор ОБЯЗАН возвращать число, а не булево значение` },
      { type: 'heading', level: 2, content: 'Сортировка строк — нужен localeCompare' },
      { type: 'code', language: 'javascript', content: `const names = ['Ёжик', 'Яблоко', 'Антон', 'елка'];

// Плохой вариант — оператор < даёт неверный порядок для кириллицы
names.sort((a, b) => a < b ? -1 : 1);
// Результат непредсказуем для ё, й, е в разных регистрах

// Правильный вариант — localeCompare с явной локалью
names.sort((a, b) => a.localeCompare(b, 'ru', { sensitivity: 'base' }));
// sensitivity: 'base' игнорирует регистр и диакритику
// → ['Антон', 'елка', 'Ёжик', 'Яблоко']

// localeCompare возвращает -1, 0 или 1 — уже готовый компаратор` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Сортировка объектов по нескольким полям — пошагово' },
      { type: 'list', content: [
        'Задача: отсортировать пользователей сначала по отделу, а при одинаковом отделе — по зарплате по убыванию.',
        'Пишем компаратор: сначала сравниваем department через localeCompare.',
        'Если localeCompare вернул не 0 — возвращаем этот результат (элементы в разных отделах).',
        'Если localeCompare вернул 0 (одинаковый отдел) — сравниваем salary: b.salary - a.salary (убывание).',
        'Такой каскадный компаратор легко расширяется на любое количество критериев.'
      ]}
    ],
    whenToUse: 'Всегда передавайте компаратор когда сортируете числа, даты, объекты или строки с кириллицей. Используйте localeCompare при сортировке текстовых полей, отображаемых пользователю.',
    whenNotToUse: 'Не используйте (a, b) => a - b для чисел очень близких к Number.MAX_SAFE_INTEGER — может возникнуть переполнение. В таких редких случаях безопаснее сравнивать через тернарный оператор.',
    rightExample: {
      title: 'Сортировка объектов по нескольким полям',
      code: `const employees = [
  { name: 'Иван',   dept: 'IT',  salary: 90000 },
  { name: 'Анна',   dept: 'HR',  salary: 70000 },
  { name: 'Петр',   dept: 'IT',  salary: 120000 },
  { name: 'Мария',  dept: 'HR',  salary: 80000 }
];

employees.sort((a, b) => {
  // 1. Сначала сортируем по отделу (алфавит)
  const deptCmp = a.dept.localeCompare(b.dept, 'ru');
  if (deptCmp !== 0) return deptCmp;

  // 2. При одинаковом отделе — по зарплате по убыванию
  return b.salary - a.salary;
});

// Результат:
// { Мария, HR, 80000 }
// { Анна,  HR, 70000 }
// { Петр,  IT, 120000 }
// { Иван,  IT, 90000 }`,
      explanation: 'Если первый компаратор вернул не 0 — элементы из разных категорий, возвращаем этот результат. Если 0 — переходим ко второму критерию. Такой паттерн легко масштабируется.'
    },
    wrongExample: {
      title: 'Сортировка чисел без компаратора — классический баг',
      code: `const numbers = [10, 5, 2, 20, 1];
numbers.sort(); // Без компаратора!
console.log(numbers); // [1, 10, 2, 20, 5]

// Что произошло:
// Все числа преобразованы в строки: '10', '5', '2', '20', '1'
// Строки сравниваются посимвольно:
// '1' < '10' < '2' < '20' < '5'
// (потому что '1' < '2' < '5' по коду Unicode)`,
      explanation: 'Без компаратора sort() преобразует числа в строки и сортирует лексикографически. Это стандартное поведение по спецификации, не баг — но типичная ловушка для новичков.'
    },
    commonMistakes: [
      'Вызов sort() без компаратора для числовых массивов — результат будет лексикографическим, не числовым.',
      'Возврат boolean из компаратора: (a, b) => a > b. Нужно возвращать число. true и false приводятся к 1 и 0, теряя информацию о знаке.',
      'Не учитывать регистр при сравнении строк без опции sensitivity в localeCompare — «банан» и «Банан» могут оказаться в разных местах списка.'
    ],
    checklist: [
      'Знать почему [10, 5, 2].sort() даёт [10, 2, 5] и как это исправить.',
      'Уметь писать компаратор для числовой сортировки по возрастанию и убыванию.',
      'Уметь сортировать массив объектов по нескольким полям в нужном порядке.',
      'Использовать localeCompare для корректной сортировки русских строк.'
    ],
    interviewQA: [
      {
        question: 'Почему вызов [10, 5, 2, 20, 1].sort() выдаёт [1, 10, 2, 20, 5]?',
        interviewerExpectation: 'Проверяется знание спецификации ECMA-262 и понимание лексикографического сравнения.',
        verbalStructure: 'Объясните поведение по умолчанию (приведение к строкам), покажите как сравниваются «10» и «2» посимвольно, предложите решение с компаратором.',
        firstMention: 'По умолчанию sort() преобразует элементы в строки. Строка «10» начинается с «1», а «2» начинается с «2». В Unicode «1» < «2», поэтому «10» идёт перед «2».',
        commonAnswerMistakes: 'Ответ «это баг JS» — это не баг, это спецификация. Или ответ без объяснения механизма лексикографического сравнения.',
        strongAnswer: 'Это стандартное поведение Array.prototype.sort(). Без компаратора он вызывает toString() на каждом элементе и сравнивает строки по кодовым точкам Unicode слева направо. Строка «10» начинается с символа «1» (код 49), а «2» начинается с «2» (код 50). Поскольку 49 < 50, «10» < «2» лексикографически. Решение — передать компаратор: sort((a, b) => a - b).',
        followUpQuestions: [
          'Как работает компаратор под капотом если он возвращает 0?',
          'Безопасно ли использовать (a, b) => a - b для чисел с плавающей точкой?'
        ]
      }
    ],
    relatedTopics: ['stability-immutability', 'production-practices'],
    quizzes: [
      {
        id: 'quiz-sort-comp-1',
        question: 'Что должен вернуть компаратор (a, b), чтобы элемент «a» расположился ПОСЛЕ «b»?',
        options: ['Отрицательное число', 'Положительное число', 'Ноль', 'Булево true'],
        correctAnswerIndex: 1,
        explanation: 'Если компаратор возвращает > 0 — алгоритм ставит b раньше a. Значит a окажется после b. Отрицательное число — a перед b. Ноль — порядок сохраняется.'
      }
    ],
    tasks: ['sort-numbers-basic', 'sort-objects']
  },
  {
    slug: 'stability-immutability',
    title: 'Стабильность сортировки и мутации',
    section: 'fundamentals',
    sectionTitle: 'Базовые знания перед сортировкой',
    order: 4,
    difficulty: 'Средний',
    estimatedMinutes: 15,
    prerequisites: ['sort-comparators'],
    summary: 'Исследование мутирующей природы метода sort(), появление нового метода toSorted() и важность стабильности алгоритма сортировки.',
    whatToKnowBefore: 'Различия между ссылочными и примитивными типами данных в JS.',
    keyTerms: [
      { term: 'Стабильная сортировка (Stable Sort)', definition: 'Алгоритм, сохраняющий исходный порядок элементов с одинаковыми ключами сортировки. Пример: Иван(22) и Иван(25) — стабильный алгоритм оставит их в исходном порядке.' },
      { term: 'In-place сортировка', definition: 'Сортировка внутри исходного массива без выделения значительной дополнительной памяти. Метод sort() работает in-place и возвращает ту же ссылку.' },
      { term: 'Shallow Comparison', definition: 'Поверхностное сравнение объектов по ссылке (адресу в памяти), а не по содержимому. React использует этот принцип для определения необходимости перерендеринга.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Метод Array.prototype.sort() сортирует массив in-place — прямо внутри исходного объекта в памяти. Он возвращает ссылку на тот же объект. Это экономит память, но нарушает иммутабельность.' },
      { type: 'code', language: 'javascript', content: `const original = [3, 1, 2];
const result = original.sort();

// sort() мутировал original и вернул ту же ссылку
console.log(original);         // [1, 2, 3] — исходный изменён!
console.log(result);           // [1, 2, 3]
console.log(original === result); // true — это ОДИН и тот же объект в памяти

// Сравните с немутирующим вариантом:
const arr = [3, 1, 2];
const sorted = [...arr].sort(); // создаём копию, затем сортируем её

console.log(arr);    // [3, 1, 2] — оригинал сохранён!
console.log(sorted); // [1, 2, 3]
console.log(arr === sorted); // false — разные объекты` },
      { type: 'heading', level: 2, content: 'Почему мутация ломает React — разбор по шагам' },
      { type: 'code', language: 'javascript', content: `// Представьте этот компонент React:
const [users, setUsers] = useState(['Пётр', 'Анна', 'Иван']);

function handleSort() {
  // ПЛОХО: мутируем state напрямую
  users.sort();
  // Массив отсортирован в памяти,
  // но ссылка на объект та же, что и раньше!

  setUsers(users);
  // React сравнивает: prevState === newState ?
  // Да! Потому что это та же ссылка.
  // React думает: "данные не изменились, рендер не нужен"
  // Баг: UI показывает старый порядок!

  // ПРАВИЛЬНО:
  setUsers([...users].sort());
  // [...users] создаёт НОВЫЙ массив → новая ссылка
  // React видит изменение → запускает рендер → UI обновляется
}` },
      { type: 'heading', level: 2, content: 'Современное решение: метод toSorted()' },
      { type: 'paragraph', content: 'С ES2023 у массивов появился метод toSorted() — иммутабельный аналог sort(). Он создаёт копию массива, сортирует её и возвращает. Оригинал не трогает. Аналогично работают toReversed() и toSpliced().' },
      { type: 'code', language: 'javascript', content: `const scores = [80, 95, 70];

// Старый способ — копировать вручную
const sorted1 = [...scores].sort((a, b) => a - b);

// Новый способ — toSorted (ES2023)
const sorted2 = scores.toSorted((a, b) => a - b);

console.log(scores);  // [80, 95, 70] — оригинал не тронут (в обоих случаях!)
console.log(sorted2); // [70, 80, 95]` },
      { type: 'heading', level: 2, content: 'Стабильность сортировки — когда это важно' },
      { type: 'paragraph', content: 'Сортировка стабильна, если элементы с одинаковым ключом сохраняют свой исходный порядок. С ES2019 JavaScript гарантирует стабильность встроенного sort(). Это важно при сортировке таблиц по нескольким колонкам поочерёдно.' },
      { type: 'code', language: 'javascript', content: `// Пример где стабильность критична:
// Сначала сортируем по городу, затем внутри каждого города — по имени

const employees = [
  { name: 'Зина',  city: 'Москва' },
  { name: 'Антон', city: 'Питер'  },
  { name: 'Борис', city: 'Москва' },
  { name: 'Анна',  city: 'Питер'  },
];

// Шаг 1: сортируем по имени
employees.sort((a, b) => a.name.localeCompare(b.name, 'ru'));
// Анна, Антон, Борис, Зина

// Шаг 2: сортируем по городу (стабильный алгоритм сохранит порядок имён внутри города)
employees.sort((a, b) => a.city.localeCompare(b.city, 'ru'));
// Анна(Питер), Антон(Питер), Борис(Москва), Зина(Москва)
// Внутри каждого города имена остались в алфавитном порядке ← это стабильность!` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Поведение ссылок при мутации массива' },
      { type: 'list', content: [
        'Создаётся массив const original = [3, 1, 2]. В переменной original хранится не массив, а адрес ячейки памяти (ссылка).',
        'Вызывается original.sort(). Алгоритм сортировки переставляет элементы прямо по этому адресу в памяти.',
        'sort() возвращает тот же адрес (ту же ссылку). Переменная result = original.sort() получает тот же адрес.',
        'Поэтому original === result возвращает true — это один и тот же объект, и original тоже «внезапно» отсортирован.'
      ]}
    ],
    whenToUse: 'Используйте toSorted() или [...arr].sort() везде, где важна иммутабельность — в React-компонентах, Redux редьюсерах, функциях обработки входных данных.',
    whenNotToUse: 'Избегайте toSorted() в горячих циклах с огромными массивами — выделение памяти под копии создаёт нагрузку на Garbage Collector. Там уместен мутирующий in-place sort().',
    rightExample: {
      title: 'Иммутабельная сортировка — правильный подход',
      code: `const scores = [80, 95, 70];

// Способ 1: spread + sort (работает везде)
const sorted1 = [...scores].sort((a, b) => a - b);

// Способ 2: toSorted() (ES2023, не поддерживается IE и старыми браузерами)
const sorted2 = scores.toSorted((a, b) => a - b);

console.log(scores);  // [80, 95, 70] — не изменился!
console.log(sorted1); // [70, 80, 95]
console.log(sorted2); // [70, 80, 95]`,
      explanation: 'В обоих случаях исходный массив scores остаётся нетронутым. Для React-компонентов это критично — только новая ссылка вызовет перерендеринг.'
    },
    wrongExample: {
      title: 'Мутация данных состояния — скрытый баг React',
      code: `const initialList = ['яблоки', 'груши', 'бананы'];
const sortedList = initialList.sort();

// Что случилось:
console.log(initialList); // ['бананы', 'груши', 'яблоки'] ← МУТИРОВАН!
console.log(initialList === sortedList); // true ← один объект!

// В React-компоненте:
// setList(initialList.sort()) — ссылка не изменилась
// → компонент НЕ перерисуется, баг в UI`,
      explanation: 'sort() мутировал исходный массив. Обе переменные теперь указывают на один изменённый объект. Исходный порядок потерян навсегда.'
    },
    commonMistakes: [
      'Вызов sort() напрямую на массиве состояния в React — ссылка не меняется, компонент не обновится.',
      'Предположение, что toSorted() поддерживается везде — метод из ES2023, в старых браузерах без полифила упадёт.',
      'Не понимать что стабильность нужна при последовательной сортировке по нескольким критериям.'
    ],
    checklist: [
      'Понимать, что sort() возвращает ту же ссылку и мутирует исходный массив.',
      'Уметь делать иммутабельную копию перед сортировкой через [...arr] или slice().',
      'Объяснить разницу между sort() и toSorted().',
      'Объяснить стабильность на примере сортировки таблицы по двум колонкам.'
    ],
    interviewQA: [
      {
        question: 'Что произойдёт если отсортировать массив состояний в React через sort()? Как исправить?',
        interviewerExpectation: 'Проверяется умение связывать фундаментальные особенности JS (мутация, ссылочные типы) с работой React (shallow comparison, reconciliation).',
        verbalStructure: 'Объясните что sort() мутирует на месте → ссылка не меняется → React не перерисует. Покажите решение через [...state].sort() или state.toSorted().',
        firstMention: 'React использует поверхностное сравнение (Object.is) для проверки изменений. sort() мутирует массив и возвращает ту же ссылку — React не заметит изменений.',
        commonAnswerMistakes: 'Ответ что React выбросит ошибку компиляции или выполнения. Или ответ про useEffect как решение.',
        strongAnswer: 'state.sort() сортирует массив на месте и возвращает ту же ссылку. React при вызове setState сравнивает prevState === newState через Object.is. Адрес объекта в памяти не изменился, значит React пропустит перерисовку. Решения: (1) [...state].sort() — spread создаёт новый массив с новой ссылкой; (2) state.toSorted() — ES2023, делает то же самое; (3) state.slice().sort() — аналог spread.',
        followUpQuestions: [
          'Как работает полифил для toSorted() в старых браузерах?',
          'Что такое deep copy и когда она нужна при сортировке массива объектов?'
        ]
      }
    ],
    relatedTopics: ['sort-comparators', 'production-practices'],
    quizzes: [
      {
        id: 'quiz-stability-1',
        question: 'Какое из выражений верно для кода: const a = [3, 2, 1]; const b = a.sort();?',
        options: ['a === b', 'a !== b', 'a.sort() возвращает новый массив', 'Исходный массив a остался [3, 2, 1]'],
        correctAnswerIndex: 0,
        explanation: 'sort() мутирует исходный массив и возвращает ссылку на него же. Поэтому a и b — это один и тот же объект в памяти, и a === b даёт true.'
      }
    ],
    tasks: ['to-sorted-immutable']
  },
  {
    slug: 'complexity-basics',
    title: 'Основы сложности алгоритмов',
    section: 'fundamentals',
    sectionTitle: 'Базовые знания перед сортировкой',
    order: 5,
    difficulty: 'Средний',
    estimatedMinutes: 20,
    prerequisites: ['arrays-iteration'],
    summary: 'Введение в оценку сложности алгоритмов Big O notation. Оценка времени работы O(1), O(n), O(n²) и O(n log n) на примерах поиска и сортировок.',
    whatToKnowBefore: 'Понимание циклов и вложенных циклов.',
    keyTerms: [
      { term: 'Нотация Big O', definition: 'Способ описать, как растёт время работы или расход памяти алгоритма при увеличении размера входных данных N. Показывает наихудший сценарий, игнорируя константы.' },
      { term: 'Временная сложность (Time Complexity)', definition: 'Количество элементарных операций (сравнений, присваиваний) которое алгоритм выполняет в зависимости от N.' },
      { term: 'Пространственная сложность (Space Complexity)', definition: 'Объём дополнительной памяти, которую алгоритм использует помимо входных данных. O(1) — константная (не зависит от N), O(n) — создаёт копию входного массива.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Big O не измеряет время в секундах — это зависит от процессора. Big O измеряет количество операций, которое растёт с ростом N. Это позволяет сравнивать алгоритмы независимо от «железа».' },
      { type: 'heading', level: 2, content: 'Основные классы сложности с реальными числами' },
      { type: 'list', content: [
        'O(1) — Константное время. Число операций не зависит от N. Пример: arr[5] — чтение по индексу всегда 1 операция, будь массив из 10 или из 10 000 000 элементов.',
        'O(n) — Линейное время. 1 цикл по всему массиву. Для N=10: ~10 операций, для N=1000: ~1000 операций. Рост прямо пропорциональный.',
        'O(n²) — Квадратичное время. 2 вложенных цикла по N. Для N=10: ~100 операций, для N=1000: ~1 000 000 операций, для N=10000: ~100 000 000 операций — становится неприемлемо медленным.',
        'O(n log n) — Оптимальное для сортировок. Для N=1000: ~10 000 операций (log₂(1000) ≈ 10). Это в 100 раз быстрее O(n²)!'
      ]},
      { type: 'heading', level: 2, content: 'Как читать код и понять сложность' },
      { type: 'code', language: 'javascript', content: `// O(1) — нет циклов, константа
function getFirst(arr) {
  return arr[0]; // 1 операция всегда
}

// O(n) — один цикл по N элементам
function findMax(arr) {
  let max = arr[0];
  for (let i = 1; i < arr.length; i++) { // N-1 итераций
    if (arr[i] > max) max = arr[i];
  }
  return max;
}

// O(n²) — вложенные циклы, оба по N
function hasDuplicates(arr) {
  for (let i = 0; i < arr.length; i++) {       // N итераций
    for (let j = i + 1; j < arr.length; j++) { // ~N/2 итераций в среднем
      if (arr[i] === arr[j]) return true; // Итого: ~N²/2 операций → O(n²)
    }
  }
  return false;
}

// O(n log n) — рекурсивное деление пополам + линейный проход на каждом уровне
// Так работает merge sort и quick sort (в среднем случае)` },
      { type: 'heading', level: 2, content: 'Почему логарифм такой эффективный' },
      { type: 'paragraph', content: 'Логарифм по основанию 2 отвечает на вопрос: сколько раз нужно разделить N на 2, чтобы получить 1? Для N=1000: делим 10 раз (1000→500→250→125→...→1). Для N=1 000 000: делим всего 20 раз! Вот почему алгоритмы с разделением пополам (бинарный поиск, merge sort) такие быстрые — их сложность растёт очень медленно.' },
      { type: 'alert', alertType: 'info', content: 'Константы отбрасываются: O(2n) = O(n), O(n²/2) = O(n²). Нас интересует только характер роста, а не точное число операций. Два последовательных цикла O(n) + O(n) = O(2n) = O(n), но не O(n²) — вложенности нет.' },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Правила определения сложности по коду' },
      { type: 'list', content: [
        '1. Считайте циклы: один цикл по N → O(n), два вложенных цикла по N → O(n²).',
        '2. Если циклы последовательные (не вложенные) → складываем и берём доминирующий: O(n) + O(n²) = O(n²).',
        '3. Встроенные методы тоже имеют сложность: filter, map, slice — это O(n). indexOf, includes — O(n). Вызов indexOf внутри цикла даёт O(n²)!',
        '4. Рекурсивные функции, делящие задачу пополам (mergeSort, quickSort) → O(n log n).',
        '5. Если нет циклов и нет рекурсии → вероятно O(1).'
      ]}
    ],
    whenToUse: 'Оценивайте сложность при проектировании функций, которые будут обрабатывать большие массивы данных (тысячи и более элементов). Особенно важно при работе на клиентской стороне — медленный алгоритм блокирует UI.',
    whenNotToUse: 'Не переусложняйте оптимизацию при малых N (< 50). O(n²) с N=20 — это 400 операций, это мгновенно. Читаемость кода важнее micro-оптимизаций на маленьких данных.',
    rightExample: {
      title: 'Поиск дубликатов за O(n) — вместо O(n²)',
      code: `// O(n²) — медленно для больших массивов
function hasDuplicatesSlow(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[i] === arr[j]) return true; // N² сравнений
    }
  }
  return false;
}

// O(n) — быстро! Используем Set для проверки за O(1)
function hasDuplicatesFast(arr) {
  const seen = new Set();
  for (const item of arr) {
    if (seen.has(item)) return true; // O(1) проверка
    seen.add(item);                  // O(1) добавление
  }
  return false;
}

// Для N=10000:
// Медленный: ~50 000 000 операций
// Быстрый:   ~10 000 операций — в 5000 раз быстрее!`,
      explanation: 'Set обеспечивает проверку за O(1) благодаря хэш-таблице. Один проход по массиву — O(n). Использование правильной структуры данных превращает O(n²) в O(n).'
    },
    wrongExample: {
      title: 'indexOf внутри цикла — скрытый O(n²)',
      code: `const arr = [3, 1, 4, 1, 5, 9, 2, 6];
const unique = [];

for (const item of arr) {
  // Ошибка: indexOf — это O(n) внутри цикла O(n) = O(n²)!
  if (!unique.indexOf(item) === -1) {
    unique.push(item);
  }
}

// Правильно: использовать Set или Map
const uniqueFast = [...new Set(arr)]; // O(n)`,
      explanation: 'indexOf проходит по всему массиву unique для каждого элемента arr. При большом массиве это становится критически медленным. Замена на Set делает проверку за O(1).'
    },
    commonMistakes: [
      'Не учитывать сложность методов внутри цикла: includes, indexOf, filter — все они O(n). Их вызов внутри цикла превращает O(n) в O(n²).',
      'Путаница временной и пространственной сложности: merge sort имеет O(n log n) по времени и O(n) по памяти — это разные вещи.',
      'Думать что O(n²) всегда плохо — для N=10 это 100 операций, абсолютно приемлемо.'
    ],
    checklist: [
      'Уметь определить сложность по структуре циклов в коде.',
      'Понимать, что O(n log n) на 1000 элементов в 100 раз лучше O(n²).',
      'Знать сложность часто используемых методов: indexOf, includes — O(n); Set.has, Map.get — O(1).'
    ],
    interviewQA: [
      {
        question: 'Какова временная сложность встроенного метода Array.prototype.sort() в JavaScript?',
        interviewerExpectation: 'Проверяется теоретическая база: знание Timsort и понимание теоретического предела сортировки сравнением.',
        verbalStructure: 'Назовите O(n log n) в худшем случае, упомяните Timsort (гибрид merge sort + insertion sort), объясните почему O(n) в лучшем случае (уже отсортированный массив).',
        firstMention: 'Временная сложность встроенного sort() — O(n log n) в среднем и худшем случаях. Алгоритм — Timsort.',
        commonAnswerMistakes: 'Ответ O(n²) или O(n) без уточнения случая.',
        strongAnswer: 'JavaScript-движки (V8 в Chrome/Node, JavaScriptCore в Safari) реализуют стабильный алгоритм Timsort — гибрид сортировки слиянием и вставками. Временная сложность: O(n log n) в худшем и среднем случаях, O(n) в лучшем (массив уже отсортирован, Timsort это детектирует). Пространственная сложность O(n) — нужен буфер для слияния. Теоретически O(n log n) — минимально достижимый предел для алгоритмов, основанных на сравнении элементов.',
        followUpQuestions: [
          'Как изменится сложность если размер массива очень мал (N < 32)?',
          'Почему O(n log n) является теоретическим минимумом для сравнительных сортировок?'
        ]
      }
    ],
    relatedTopics: ['arrays-iteration', 'bubble-sort'],
    quizzes: [
      {
        id: 'quiz-complexity-1',
        question: 'Какова сложность функции с двумя ПОСЛЕДОВАТЕЛЬНЫМИ (не вложенными) циклами по N элементов?',
        options: ['O(1)', 'O(n)', 'O(n²)', 'O(2n)'],
        correctAnswerIndex: 1,
        explanation: 'O(n) + O(n) = O(2n), но константа 2 отбрасывается → O(n). Сложность перемножается только при ВЛОЖЕННЫХ циклах.'
      }
    ],
    tasks: []
  },
  {
    slug: 'bubble-sort',
    title: 'Пузырьковая сортировка (Bubble Sort)',
    section: 'algorithms',
    sectionTitle: 'Алгоритмы сортировки для интервью',
    order: 6,
    difficulty: 'Легкий',
    estimatedMinutes: 20,
    prerequisites: ['complexity-basics', 'sort-comparators'],
    summary: 'Изучение классического алгоритма пузырьковой сортировки. Пошаговая трассировка, разбор каждой строки кода, программная реализация на JS и оптимизации.',
    whatToKnowBefore: 'Уверенное понимание вложенных циклов и механизма обмена значений (swap).',
    keyTerms: [
      { term: 'Обмен значений (Swap)', definition: 'Операция обмена значений двух переменных. В современном JS делается через деструктуризацию: [a, b] = [b, a]. Старый способ — через временную переменную temp.' },
      { term: 'Всплытие элемента', definition: 'После каждого полного прохода пузырьковой сортировки наибольший неотсортированный элемент перемещается на своё финальное место в правой части — как пузырёк воздуха поднимается вверх.' },
      { term: 'Инвариант цикла', definition: 'Условие, которое гарантированно истинно ПОСЛЕ каждого прохода. Для пузырьковой сортировки: после прохода i правые i элементов уже стоят на своих финальных местах.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Пузырьковая сортировка — самый простой для понимания алгоритм. На каждом шаге мы сравниваем два соседних элемента и меняем их местами, если левый больше правого. В результате каждого полного прохода самый большой из оставшихся элементов «всплывает» в конец неотсортированной части.' },
      { type: 'heading', level: 2, content: 'Разбор каждой строки кода — что и зачем' },
      { type: 'code', language: 'javascript', content: `function bubbleSort(arr) {
  const n = arr.length;
  //  ^ Сохраняем в переменную, чтобы не пересчитывать arr.length на каждой итерации

  for (let i = 0; i < n; i++) {
    //              ^ Внешний цикл — считает пройденные «проходы».
    //                После каждого прохода ещё один элемент занял финальное место.

    let swapped = false;
    //  ^ Флаг оптимизации: если за весь проход не было ни одного обмена,
    //    массив уже отсортирован — можно выйти досрочно.

    for (let j = 0; j < n - 1 - i; j++) {
      //              ^^^^^^^^^^^^^^^^^
      //              Почему n - 1 - i? Разбираем:
      //
      //  • n - 1: если j дойдёт до n-1 (последнего индекса),
      //    то arr[j+1] = arr[n] — это выход за границы массива!
      //    Поэтому j идёт максимум до n-2, то есть j < n-1.
      //
      //  • - i: после i-го прохода ПОСЛЕДНИЕ I ЭЛЕМЕНТОВ уже
      //    отсортированы и стоят на своих местах. Нет смысла
      //    их снова проверять. Каждый проход сокращает диапазон на 1.
      //
      //  Пример для массива [5,3,1,4] (n=4):
      //    Проход 0 (i=0): j от 0 до 4-1-0=3, то есть j < 3 (индексы 0,1,2)
      //    Проход 1 (i=1): j от 0 до 4-1-1=2, то есть j < 2 (индексы 0,1)
      //    Проход 2 (i=2): j от 0 до 4-1-2=1, то есть j < 1 (индекс 0)

      if (arr[j] > arr[j + 1]) {
        //         ^ Сравниваем текущий элемент с СОСЕДНИМ справа
        [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
        //  ^ Обмен через деструктуризацию — современный и читаемый способ
        swapped = true; // Отмечаем что обмен был
      }
    }

    if (!swapped) break;
    // ^ Если за весь проход не было обменов — массив отсортирован.
    //   Досрочный выход. Для отсортированного массива даёт O(n).
  }
  return arr;
}` },
      { type: 'heading', level: 2, content: 'Визуализация: как работает условие j < n - 1 - i' },
      { type: 'code', language: 'javascript', content: `// Массив [4, 3, 1, 2], n = 4
//
// ПРОХОД i=0: j < 4-1-0 = 3, значит j = 0,1,2
//   j=0: сравниваем 4 и 3. 4>3 → обмен → [3, 4, 1, 2]
//   j=1: сравниваем 4 и 1. 4>1 → обмен → [3, 1, 4, 2]
//   j=2: сравниваем 4 и 2. 4>2 → обмен → [3, 1, 2, 4]
//   Результат: [3, 1, 2, 4]. Максимум «4» всплыл на последнее место ✓
//
// ПРОХОД i=1: j < 4-1-1 = 2, значит j = 0,1
//   (индекс j=2 пропускаем — там уже стоит «4», финальная позиция)
//   j=0: сравниваем 3 и 1. 3>1 → обмен → [1, 3, 2, 4]
//   j=1: сравниваем 3 и 2. 3>2 → обмен → [1, 2, 3, 4]
//   Результат: [1, 2, 3, 4]. «3» всплыл на своё место ✓
//
// ПРОХОД i=2: j < 4-1-2 = 1, значит j = 0
//   j=0: сравниваем 1 и 2. 1<2 → обмена нет
//   swapped = false → досрочный выход! Массив отсортирован ✓` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Пошаговый пример сортировки массива [5, 1, 4]' },
      { type: 'list', content: [
        'Начало: [5, 1, 4]. n = 3.',
        'Проход i=0, j < 2 (то есть j = 0 и 1):',
        '  j=0: сравниваем arr[0]=5 и arr[1]=1. 5 > 1 → обмен → [1, 5, 4]',
        '  j=1: сравниваем arr[1]=5 и arr[2]=4. 5 > 4 → обмен → [1, 4, 5]',
        '  После прохода 0: элемент «5» стоит на финальной позиции (индекс 2).',
        'Проход i=1, j < 1 (то есть только j = 0):',
        '  j=0: сравниваем arr[0]=1 и arr[1]=4. 1 < 4 → обмена нет.',
        '  swapped = false → досрочный выход. Массив [1, 4, 5] уже отсортирован!'
      ]}
    ],
    whenToUse: 'Используйте пузырьковую сортировку исключительно в учебных целях для развития алгоритмического мышления. Плюс — идеальна для объяснения концепции сортировки из-за своей простоты.',
    whenNotToUse: 'Никогда не используйте в реальных проектах. O(n²) означает, что для 10 000 элементов будет 50 000 000 операций. Встроенный sort() справится в 500 раз быстрее.',
    rightExample: {
      title: 'Оптимизированная пузырьковая сортировка с разбором каждой строки',
      code: `function bubbleSort(arr) {
  const n = arr.length;
  for (let i = 0; i < n; i++) {
    let swapped = false;
    // j < n - 1 - i:
    //   n-1: не выходить за пределы при обращении к arr[j+1]
    //   -i:  пропускаем уже отсортированные последние i элементов
    for (let j = 0; j < n - 1 - i; j++) {
      if (arr[j] > arr[j + 1]) {
        [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
        swapped = true;
      }
    }
    if (!swapped) break; // Досрочный выход если уже отсортировано
  }
  return arr;
}

// Тест:
console.log(bubbleSort([5, 3, 1, 4, 2])); // [1, 2, 3, 4, 5]`,
      explanation: 'Условие j < n - 1 - i — сердце оптимизации: (n-1) защищает от выхода за границы при arr[j+1], а (-i) исключает из проверки уже отсортированный хвост. Флаг swapped даёт O(n) на уже отсортированном массиве.'
    },
    wrongExample: {
      title: 'Неэффективная реализация без оптимизаций',
      code: `function badBubbleSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    // Ошибка 1: j < arr.length - 1 не учитывает что хвост уже отсортирован
    // При i=1 всё равно проверяем последний элемент — это лишняя работа
    for (let j = 0; j < arr.length - 1; j++) {
      if (arr[j] > arr[j + 1]) {
        const temp = arr[j];
        arr[j] = arr[j + 1];
        arr[j + 1] = temp;
        // Ошибка 2: нет флага swapped — даже для отсортированного массива
        // пройдём все N² итераций
      }
    }
  }
  return arr;
}`,
      explanation: 'Два недостатка: 1) внутренний цикл проверяет весь массив, не зная что правая часть уже отсортирована — лишние сравнения; 2) нет ранней остановки — для отсортированного массива выполнит все N² операций вместо N.'
    },
    commonMistakes: [
      'Написать j < n вместо j < n - 1 — тогда arr[j+1] при j = n-1 обратится к arr[n], которого нет, и получим undefined в сравнении.',
      'Забыть вычесть i из условия (писать j < n - 1 вместо j < n - 1 - i) — алгоритм работает, но медленнее, проверяя уже отсортированный хвост.',
      'Не использовать флаг swapped — алгоритм всегда делает N проходов, даже если массив отсортирован после первого.'
    ],
    checklist: [
      'Уметь объяснить почему условие j < n - 1 - i именно такое (обе части: n-1 и -i).',
      'Написать алгоритм по памяти, включая оптимизацию с флагом swapped.',
      'Уметь трассировать алгоритм на массиве из 4-5 элементов вручную.'
    ],
    interviewQA: [
      {
        question: 'Какова временная и пространственная сложность пузырьковой сортировки? Что даёт оптимизация с флагом swapped?',
        interviewerExpectation: 'Проверяется знание граничных случаев сложности и умение объяснять оптимизации.',
        verbalStructure: 'Назовите худший случай O(n²), объясните лучший O(n) с флагом, укажите O(1) по памяти. Объясните что такое «уже отсортированный массив» как лучший случай.',
        firstMention: 'Худший и средний случаи — O(n²). С оптимизацией swapped: лучший случай (уже отсортированный массив) — O(n). Память — O(1).',
        commonAnswerMistakes: 'Ответ что пространственная сложность O(n) «из-за обменов» — нет, обмены происходят in-place, доп. память не выделяется.',
        strongAnswer: 'В худшем случае (обратный порядок) пузырьковая сортировка делает (n-1)+(n-2)+...+1 = n(n-1)/2 сравнений → O(n²). В лучшем случае (массив отсортирован) с флагом swapped: первый проход делает n-1 сравнений, флаг не меняется, алгоритм завершается → O(n). Пространственная сложность O(1) — работает in-place, дополнительная память только для переменных i, j, swapped.',
        followUpQuestions: [
          'Является ли пузырьковая сортировка стабильной и почему?',
          'При каких условиях пузырьковая сортировка практически эффективна?'
        ]
      }
    ],
    relatedTopics: ['complexity-basics', 'selection-sort'],
    quizzes: [
      {
        id: 'quiz-bubble-1',
        question: 'Почему во внутреннем цикле пузырьковой сортировки условие j < n - 1 - i, а не просто j < n?',
        options: [
          'Потому что n - 1 - i быстрее вычисляется',
          'n-1 защищает от выхода за границы при arr[j+1], а -i пропускает уже отсортированный хвост',
          'Чтобы внешний цикл мог считать правильно',
          'Это просто стандартный шаблон без особого смысла'
        ],
        correctAnswerIndex: 1,
        explanation: 'n-1: если j достигнет n-1, обращение arr[j+1] = arr[n] выйдет за границы. -i: после i проходов последние i элементов стоят на финальных местах — проверять их снова нет смысла.'
      }
    ],
    tasks: ['bubble-sort-task']
  },
  {
    slug: 'selection-sort',
    title: 'Сортировка выбором (Selection Sort)',
    section: 'algorithms',
    sectionTitle: 'Алгоритмы сортировки для интервью',
    order: 7,
    difficulty: 'Легкий',
    estimatedMinutes: 20,
    prerequisites: ['bubble-sort'],
    summary: 'Изучение алгоритма сортировки выбором. Принцип поиска минимального элемента, разбор каждой строки кода и сравнение с пузырьковой сортировкой.',
    whatToKnowBefore: 'Понимание алгоритма поиска минимального числа в массиве и его индекса.',
    keyTerms: [
      { term: 'Неотсортированная часть', definition: 'Подмассив, элементы которого ещё не заняли финальную позицию. В selection sort граница сдвигается слева направо на каждом проходе.' },
      { term: 'Индекс минимума (minIndex)', definition: 'Переменная, хранящая индекс наименьшего найденного элемента в неотсортированной части. После полного прохода совершается один обмен.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Сортировка выбором — второй простой алгоритм после пузырьковой. Идея: делим массив на две части — уже отсортированную (слева) и ещё нет (справа). На каждом шаге находим минимальный элемент в неотсортированной части и ставим его на следующую позицию отсортированной.' },
      { type: 'heading', level: 2, content: 'Разбор кода — что делает каждая строка' },
      { type: 'code', language: 'javascript', content: `function selectionSort(arr) {
  const n = arr.length;

  for (let i = 0; i < n - 1; i++) {
    //              ^^^^^^^^
    //  Почему n - 1, а не n?
    //  Когда i = n-1, остался последний элемент — он уже на правильном месте.
    //  Внутренний цикл с j = n начинается за границей — нечего искать.
    //  Поэтому внешний цикл заканчивается раньше — i < n-1.

    let minIndex = i;
    //  ^ Предполагаем что минимум — первый элемент неотсортированной части.
    //    Храним ИНДЕКС, а не значение! Потом сделаем один обмен по этому индексу.

    for (let j = i + 1; j < n; j++) {
      //         ^^^^^
      //  Внутренний цикл начинается с i + 1, а не с 0 или с i.
      //  - С 0: мы бы заново искали минимум в уже отсортированной части (лишняя работа).
      //  - С i: сравнивали бы arr[i] с самим собой (бессмысленно).
      //  - С i+1: смотрим только на неотсортированную часть — правильно!

      if (arr[j] < arr[minIndex]) {
        minIndex = j;
        // ^ Нашли новый минимум — запоминаем его ИНДЕКС.
        //   НЕ делаем обмен сейчас! Это главное отличие от пузырьковой сортировки.
      }
    }

    if (minIndex !== i) {
      // ^ Обмен нужен только если минимум не уже стоит на месте i.
      //   Это оптимизация: избегаем лишней записи arr[i] = arr[i].
      [arr[i], arr[minIndex]] = [arr[minIndex], arr[i]];
    }
    // Теперь arr[i] — правильный минимум неотсортированной части.
    // Граница отсортированной части сдвинулась вправо на 1.
  }
  return arr;
}` },
      { type: 'heading', level: 2, content: 'Ключевое отличие от пузырьковой сортировки' },
      { type: 'paragraph', content: 'Пузырьковая: делает обмен на каждом шаге сравнения — может быть много обменов за один проход. Сортировка выбором: накапливает информацию о минимуме (только обновляет minIndex), делает ровно ОДИН обмен в конце каждого прохода. Это делает её предпочтительной когда операция записи в память дорогостоящая (например, при работе с Flash-памятью или внешними устройствами).' },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Трассировка для массива [29, 10, 14]' },
      { type: 'list', content: [
        'Начало: [29, 10, 14]. n = 3.',
        'i=0: minIndex = 0 (значение 29). Ищем минимум в [10, 14]:',
        '  j=1: arr[1]=10 < arr[minIndex]=29 → minIndex = 1',
        '  j=2: arr[2]=14 < arr[minIndex]=10? Нет.',
        '  minIndex=1 ≠ i=0 → обмен arr[0] и arr[1] → [10, 29, 14]',
        'i=1: minIndex = 1 (значение 29). Ищем минимум в [14]:',
        '  j=2: arr[2]=14 < arr[minIndex]=29 → minIndex = 2',
        '  minIndex=2 ≠ i=1 → обмен arr[1] и arr[2] → [10, 14, 29]',
        'i=2: внешний цикл завершён (i < n-1 = 2). Итог: [10, 14, 29] ✓'
      ]}
    ],
    whenToUse: 'Используйте сортировку выбором когда операция записи (swap) значительно дороже операции чтения — алгоритм гарантирует максимум N-1 обменов.',
    whenNotToUse: 'Не используйте если нужна стабильная сортировка — классический selection sort нестабилен из-за дальних обменов.',
    rightExample: {
      title: 'Реализация сортировки выбором с подробными комментариями',
      code: `function selectionSort(arr) {
  const n = arr.length;
  
  for (let i = 0; i < n - 1; i++) {     // до n-1: последний эл. уже на месте
    let minIndex = i;                     // начинаем с первого неотсортированного
    
    for (let j = i + 1; j < n; j++) {   // ищем в неотсортированной части
      if (arr[j] < arr[minIndex]) {
        minIndex = j;                     // запоминаем индекс нового минимума
      }
    }
    
    if (minIndex !== i) {               // обмен только если нашли меньший
      [arr[i], arr[minIndex]] = [arr[minIndex], arr[i]];
    }
  }
  return arr;
}

console.log(selectionSort([64, 25, 12, 22, 11]));
// [11, 12, 22, 25, 64]`,
      explanation: 'Внутренний цикл начинается с i+1 — пропускаем уже отсортированную левую часть. Обмен делаем один раз в конце прохода, а не при каждом сравнении — это минимизирует запись в память.'
    },
    wrongExample: {
      title: 'Обмен внутри внутреннего цикла — теряем главное преимущество',
      code: `function badSelectionSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[j] < arr[i]) {
        // Ошибка: делаем обмен при КАЖДОМ нахождении меньшего элемента
        // Это не selection sort! Это ближе к пузырьковой сортировке
        // и делает множество лишних записей в память
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
    }
  }
  return arr;
}`,
      explanation: 'При нахождении каждого «нового минимума» мы сразу делаем обмен — это неправильно. Нужно сначала найти АБСОЛЮТНЫЙ минимум всей неотсортированной части, и только потом сделать один обмен.'
    },
    commonMistakes: [
      'Начинать внутренний цикл с 0 вместо i+1 — будем повторно искать минимум в уже отсортированной части, тратя вдвое больше времени.',
      'Инициализировать minIndex = 0 вместо minIndex = i — это баг: минимум всегда будет «искаться» от начала массива, не от текущей границы.',
      'Делать обмен внутри внутреннего цикла — теряется главное преимущество: минимальное число обменов.'
    ],
    checklist: [
      'Знать почему внешний цикл идёт до n-1, а не n.',
      'Понимать почему внутренний цикл начинается с i+1.',
      'Объяснить разницу с пузырьковой: один обмен за проход vs. много обменов.'
    ],
    interviewQA: [
      {
        question: 'Почему классическая сортировка выбором считается нестабильной? Приведите пример.',
        interviewerExpectation: 'Интервьюер оценивает понимание стабильности через конкретный контрпример.',
        verbalStructure: 'Определите стабильность, покажите пример где обмен через дальние позиции нарушает порядок одинаковых элементов.',
        firstMention: 'Нестабильна, потому что обмен с удалённым элементом может переставить одинаковые элементы.',
        commonAnswerMistakes: 'Ответ что «все O(n²) алгоритмы нестабильны» — неверно: пузырьковая и сортировка вставками стабильны.',
        strongAnswer: 'Рассмотрим [2a, 2b, 1], где 2a и 2b — одинаковые числа 2 в исходном порядке. На первом проходе минимум — 1 (индекс 2). Алгоритм меняет arr[0]=2a и arr[2]=1 местами → [1, 2b, 2a]. Теперь 2b идёт перед 2a — исходный порядок нарушен. Это делает алгоритм нестабильным.',
        followUpQuestions: [
          'Как сделать сортировку выбором стабильной? (Подсказка: вставка вместо обмена)',
          'Какова сложность в лучшем случае на уже отсортированном массиве?'
        ]
      }
    ],
    relatedTopics: ['bubble-sort', 'insertion-sort'],
    quizzes: [
      {
        id: 'quiz-selection-1',
        question: 'Какова временная сложность сортировки выбором в лучшем случае (уже отсортированный массив)?',
        options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(1)'],
        correctAnswerIndex: 2,
        explanation: 'В отличие от пузырьковой, selection sort не имеет механизма раннего выхода. Даже для уже отсортированного массива он всегда выполняет внутренний цикл поиска минимума для каждого i — итого O(n²).'
      }
    ],
    tasks: ['selection-sort-task']
  },
  {
    slug: 'insertion-sort',
    title: 'Сортировка вставками (Insertion Sort)',
    section: 'algorithms',
    sectionTitle: 'Алгоритмы сортировки для интервью',
    order: 8,
    difficulty: 'Средний',
    estimatedMinutes: 20,
    prerequisites: ['selection-sort'],
    summary: 'Изучение алгоритма сортировки вставками с разбором каждой строки. Метод сдвига, цикл while и его условие, эффективность на почти отсортированных данных.',
    whatToKnowBefore: 'Понимание работы цикла while и операций записи в массив.',
    keyTerms: [
      { term: 'Ключ (Key)', definition: 'В контексте insertion sort — элемент, который мы вставляем на правильную позицию. Временно сохраняется в переменную, пока другие элементы сдвигаются вправо.' },
      { term: 'Сдвиг (Shift)', definition: 'Операция перезаписи arr[j+1] = arr[j] — двигаем элемент вправо на одну позицию, освобождая место для вставки ключа. Это 1 операция записи, в отличие от swap (3 операции).' }
    ],
    theory: [
      { type: 'paragraph', content: 'Представьте что вы сортируете карты в руке. Берёте следующую карту и вставляете её на правильное место среди уже отсортированных карт, сдвигая нужные карты вправо. Именно так работает insertion sort.' },
      { type: 'heading', level: 2, content: 'Детальный разбор кода — почему каждая строка именно такая' },
      { type: 'code', language: 'javascript', content: `function insertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {
    //         ^
    //  Начинаем с 1, а не с 0!
    //  Первый элемент arr[0] считается «уже отсортированным подмассивом» из 1 элемента.
    //  Нам нужен сосед слева для сравнения, поэтому минимальный i = 1.

    const key = arr[i];
    //    ^ Сохраняем текущий элемент ОТДЕЛЬНО в переменную.
    //      Почему? Сейчас мы начнём сдвигать элементы вправо, занимая позицию arr[i].
    //      Если не сохранить — значение будет перезаписано и потеряно!

    let j = i - 1;
    //      ^^^^^ Начинаем проверку с элемента СЛЕВА от key.

    while (j >= 0 && arr[j] > key) {
    //     ^^^^^    ^^^^^^^^^^^^^^
    //     |         Условие сдвига: arr[j] должен быть БОЛЬШЕ key.
    //     |         Если arr[j] <= key — нашли нужную позицию, прекращаем.
    //     |
    //     Граничное условие: j >= 0 защищает от выхода за левую границу.
    //     Без этого при j = -1: arr[-1] = undefined, сравнение ломается.

      arr[j + 1] = arr[j];
      //  ^ Сдвигаем элемент вправо на 1 позицию. Это НЕ обмен!
      //    1 операция записи вместо 3 (как при swap).
      //    При этом arr[j+1] «перезаписывается», но это не страшно —
      //    либо там был key (сохранён в переменной), либо дубль arr[j].

      j--;
      //^ Двигаемся левее, продолжаем проверку.
    }

    arr[j + 1] = key;
    //  ^^^^^ После выхода из while, j указывает на элемент <= key (или j = -1).
    //        Значит key нужно вставить на позицию j + 1.
    //        Почему j+1? Потому что последний сдвинутый элемент теперь на j+1,
    //        а j+1 — это и есть свободное место для key.
  }
  return arr;
}` },
      { type: 'heading', level: 2, content: 'Почему сдвиг быстрее swap' },
      { type: 'code', language: 'javascript', content: `// Swap (обмен) — 3 операции записи:
const temp = arr[j];      // Запись в temp
arr[j] = arr[j-1];        // Запись в arr[j]
arr[j-1] = temp;          // Запись в arr[j-1]

// Shift (сдвиг) — 1 операция записи:
arr[j + 1] = arr[j];      // Только одна запись!
// И в конце одна финальная запись:
arr[j + 1] = key;

// Итого для вставки элемента на k позиций назад:
// Через swap: 3k операций записи
// Через shift: k + 1 операций записи
// На почти отсортированных данных это заметная разница!` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Трассировка для массива [8, 5, 2]' },
      { type: 'list', content: [
        'Начало: [8, 5, 2]. Индекс 0 (значение 8) — отсортированный подмассив.',
        'i=1: key = arr[1] = 5. j = 0.',
        '  while: j=0 >= 0? Да. arr[0]=8 > key=5? Да.',
        '  Сдвиг: arr[1] = arr[0] = 8. Массив: [8, 8, 2]. j = -1.',
        '  while: j=-1 >= 0? НЕТ → выход.',
        '  Вставка: arr[j+1] = arr[0] = 5. Массив: [5, 8, 2].',
        'i=2: key = arr[2] = 2. j = 1.',
        '  while: j=1 >= 0? Да. arr[1]=8 > 2? Да. Сдвиг: arr[2] = 8. j = 0.',
        '  while: j=0 >= 0? Да. arr[0]=5 > 2? Да. Сдвиг: arr[1] = 5. j = -1.',
        '  while: j=-1 >= 0? НЕТ → выход.',
        '  Вставка: arr[j+1] = arr[0] = 2. Итог: [2, 5, 8] ✓'
      ]}
    ],
    whenToUse: 'Идеален для маленьких массивов (< 15-20 элементов) и для почти отсортированных данных (добавление новых элементов в уже отсортированный список). Используется как вспомогательный алгоритм в Timsort.',
    whenNotToUse: 'Избегайте для больших неотсортированных массивов — O(n²) делает его непригодным для N > 1000 там, где важна скорость.',
    rightExample: {
      title: 'Реализация сортировки вставками с объяснениями',
      code: `function insertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {  // с 1: первый эл. уже "отсортирован"
    const key = arr[i]; // сохраняем перед сдвигами
    let j = i - 1;

    // Сдвигаем элементы больше key вправо
    // j >= 0: не выходим за левую границу
    // arr[j] > key: нашли нужную позицию — выходим
    while (j >= 0 && arr[j] > key) {
      arr[j + 1] = arr[j]; // сдвиг вправо (1 запись, не 3!)
      j--;
    }

    arr[j + 1] = key; // вставляем на освободившееся место
  }
  return arr;
}

console.log(insertionSort([8, 5, 2, 9, 1])); // [1, 2, 5, 8, 9]`,
      explanation: 'Сохранение key в переменную критично — без него значение будет перезаписано при первом сдвиге. Сдвиг arr[j+1] = arr[j] эффективнее swap: одна запись вместо трёх.'
    },
    wrongExample: {
      title: 'Swap вместо shift — алгоритм работает, но медленнее',
      code: `function slowInsertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {
    let j = i;
    // Используем swap вместо сдвига — работает, но в 3 раза больше записей!
    while (j > 0 && arr[j] < arr[j - 1]) {
      [arr[j], arr[j - 1]] = [arr[j - 1], arr[j]]; // 3 операции записи
      j--;
    }
  }
  return arr;
}`,
      explanation: 'Это рабочий код, но неоптимальный: каждый шаг назад — это 3 операции записи (swap) вместо 1 (shift). На почти отсортированных массивах из тысяч элементов разница становится заметной.'
    },
    commonMistakes: [
      'Забыть сохранить key = arr[i] до начала сдвигов — значение будет перезаписано при arr[j+1] = arr[j], когда j+1 = i.',
      'Написать j > 0 вместо j >= 0 — первый элемент (индекс 0) никогда не будет проверен, ключ никогда не вставится перед ним.',
      'Начать внешний цикл с i = 0 вместо i = 1 — при i=0: j = -1, цикл while сразу не выполнится, лишняя итерация.'
    ],
    checklist: [
      'Понять зачем сохранять key в переменную до сдвигов.',
      'Объяснить условие while (j >= 0 && arr[j] > key) — обе части и что будет без каждой.',
      'Написать алгоритм по памяти без подсказок.'
    ],
    interviewQA: [
      {
        question: 'Почему сортировка вставками используется в Timsort (встроенный алгоритм JS) для маленьких подмассивов?',
        interviewerExpectation: 'Проверяется знание практики и понимание накладных расходов алгоритмов.',
        verbalStructure: 'Объясните O(n) на почти отсортированных данных, отсутствие рекурсии и высокую локальность кэша на маленьких N.',
        firstMention: 'На маленьких N (< 32) insertion sort быстрее merge sort из-за отсутствия рекурсии и лучшей работы с кэшем процессора.',
        commonAnswerMistakes: 'Ответ «insertion sort всегда быстрее merge sort» — только для маленьких N.',
        strongAnswer: 'Timsort делит массив на «runs» (подмассивы) и сортирует каждый insertion sort, если run короткий. Причины: (1) нет рекурсивных вызовов — нет накладных расходов стека; (2) работает локально — данные остаются в кэше процессора L1/L2; (3) O(n) на почти отсортированных данных — а маленькие подмассивы часто уже частично упорядочены в реальных данных. После сортировки runов они объединяются через merge sort.',
        followUpQuestions: [
          'Является ли сортировка вставками стабильной и почему?',
          'Какова пространственная сложность?'
        ]
      }
    ],
    relatedTopics: ['selection-sort', 'quick-sort'],
    quizzes: [
      {
        id: 'quiz-insertion-1',
        question: 'Что произойдёт если в insertion sort убрать условие j >= 0 из while?',
        options: [
          'Алгоритм станет быстрее',
          'Ничего не изменится',
          'При j = -1 будет arr[-1] = undefined, сравнение с key может дать неверный результат',
          'Произойдёт ошибка компиляции'
        ],
        correctAnswerIndex: 2,
        explanation: 'В JS обращение arr[-1] не вызывает ошибку, но возвращает undefined. undefined > любого числа = false. Это может прекратить цикл «случайно», а не по правильной логике — баг трудно заметить.'
      }
    ],
    tasks: ['insertion-sort-task']
  },
  {
    slug: 'quick-sort',
    title: 'Быстрая сортировка (Quick Sort)',
    section: 'algorithms',
    sectionTitle: 'Алгоритмы сортировки для интервью',
    order: 9,
    difficulty: 'Сложный',
    estimatedMinutes: 30,
    prerequisites: ['complexity-basics', 'insertion-sort'],
    summary: 'Глубокий разбор алгоритма быстрой сортировки. Выбор опорного элемента, схема разбиения Ломуто с разбором каждого шага, рекурсия и защита от деградации.',
    whatToKnowBefore: 'Уверенное владение рекурсией и понимание пространственной сложности стека вызовов.',
    keyTerms: [
      { term: 'Опорный элемент (Pivot)', definition: 'Элемент, относительно которого массив разбивается на две части: всё меньше pivot — влево, всё больше — вправо. После разбиения pivot стоит на своей финальной позиции.' },
      { term: 'Partition (Разбиение)', definition: 'Ключевая операция quick sort: перераспределяет элементы вокруг pivot за O(n) времени. После partition pivot на нужном месте, левее — всё меньше, правее — всё больше.' },
      { term: 'Разделяй и властвуй', definition: 'Парадигма алгоритмов: разбить задачу на подзадачи того же типа, решить каждую рекурсивно, объединить результаты. Quick sort, merge sort — классические примеры.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Быстрая сортировка — один из самых используемых алгоритмов на практике. Работает по принципу «разделяй и властвуй»: выбираем опорный элемент, перераспределяем массив вокруг него, затем рекурсивно сортируем каждую часть.' },
      { type: 'heading', level: 2, content: 'Разбор функции partition (схема Ломуто) по шагам' },
      { type: 'code', language: 'javascript', content: `function partition(arr, left, right) {
  const pivot = arr[right];
  // ^ Выбираем ПОСЛЕДНИЙ элемент как pivot.
  //   Простой выбор, но опасен на отсортированных данных (деградация до O(n²)).

  let i = left - 1;
  // ^ Указатель i отслеживает правую границу «зоны меньших элементов».
  //   Начинаем с left - 1 (ещё ни одного элемента в зоне).
  //   После каждого нахождения элемента < pivot, i++, и элемент перемещается в зону.

  for (let j = left; j < right; j++) {
    // ^ j сканирует весь массив от left до right-1 (pivot не сканируем).

    if (arr[j] <= pivot) {
      i++;
      // ^ Расширяем зону меньших элементов на 1.
      [arr[i], arr[j]] = [arr[j], arr[i]];
      // ^ Перемещаем текущий элемент в конец зоны меньших.
    }
    // Если arr[j] > pivot — просто продолжаем, j++.
  }

  // Все элементы обработаны.
  // i указывает на последний элемент в зоне меньших.
  // Ставим pivot на место i+1 (первый элемент в зоне больших).
  [arr[i + 1], arr[right]] = [arr[right], arr[i + 1]];

  return i + 1;
  // ^ Возвращаем финальный индекс pivot.
  //   Элементы arr[left..i+1-1] <= pivot
  //   Элементы arr[i+1+1..right] > pivot
}` },
      { type: 'heading', level: 2, content: 'Полная трассировка partition на примере' },
      { type: 'code', language: 'javascript', content: `// arr = [3, 8, 2, 5], left = 0, right = 3
// pivot = arr[3] = 5
// i = left - 1 = -1

// j=0: arr[0]=3 <= pivot=5 → i=0, swap arr[0] и arr[0] → [3, 8, 2, 5]
// j=1: arr[1]=8 > pivot=5  → ничего не делаем
// j=2: arr[2]=2 <= pivot=5 → i=1, swap arr[1] и arr[2] → [3, 2, 8, 5]

// Цикл завершён. Ставим pivot на место:
// swap arr[i+1]=arr[2] и arr[right]=arr[3] → [3, 2, 5, 8]
//
// return i + 1 = 2 — это финальная позиция pivot (5)!
//
// Теперь:  [3, 2]  |  5  |  [8]
//          ^^^^^         ^^^^^
//          левая часть  правая часть — оба подмассива рекурсивно сортируем` },
      { type: 'heading', level: 2, content: 'Худший случай O(n²) — когда и почему' },
      { type: 'paragraph', content: 'Если всегда выбирать последний элемент как pivot, а массив уже отсортирован — каждый раз partition делит на части размером 0 и N-1. Глубина рекурсии становится N, а не log N. Это и есть деградация до O(n²). Защита — случайный выбор pivot или «медиана трёх».' },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Рекурсивная структура quickSort' },
      { type: 'list', content: [
        'quickSort([3,8,2,5]): pivot=5, partition → [3,2,5,8], pivotIndex=2',
        'quickSort([3,2], 0, 1): pivot=2, partition → [2,3], pivotIndex=1',
        '  quickSort([2], 0, 0): left >= right → base case, возврат',
        '  quickSort([3], 2, 1): left > right → base case, возврат',
        'quickSort([8], 3, 3): left >= right → base case, возврат',
        'Итог: [2, 3, 5, 8] — каждый рекурсивный вызов сортирует подмассив'
      ]}
    ],
    whenToUse: 'Используйте быструю сортировку для общей сортировки массивов в оперативной памяти, когда важна скорость в среднем случае и нет жёстких требований к стабильности.',
    whenNotToUse: 'Не используйте если критически важна стабильность (классический in-place Quick Sort нестабилен). Используйте с рандомизацией pivot — иначе O(n²) на отсортированных данных.',
    rightExample: {
      title: 'In-place реализация Quick Sort (схема Ломуто)',
      code: `function quickSort(arr, left = 0, right = arr.length - 1) {
  if (left >= right) return arr; // базовый случай: 0 или 1 элемент

  const pivotIndex = partition(arr, left, right);
  quickSort(arr, left, pivotIndex - 1);  // сортируем левую часть
  quickSort(arr, pivotIndex + 1, right); // сортируем правую часть
  return arr;
}

function partition(arr, left, right) {
  const pivot = arr[right]; // берём последний как pivot
  let i = left - 1;

  for (let j = left; j < right; j++) {
    if (arr[j] <= pivot) {
      i++;
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
  }

  [arr[i + 1], arr[right]] = [arr[right], arr[i + 1]];
  return i + 1; // финальная позиция pivot
}

console.log(quickSort([3, 6, 8, 10, 1, 2, 1]));
// [1, 1, 2, 3, 6, 8, 10]`,
      explanation: 'partition перераспределяет элементы вокруг pivot за O(n) и возвращает финальный индекс pivot. quickSort рекурсивно обрабатывает обе части. Ключевое: pivot после partition всегда на своём финальном месте.'
    },
    wrongExample: {
      title: 'Функциональная реализация с выделением памяти — O(n²) по памяти',
      code: `function slowQuickSort(arr) {
  if (arr.length <= 1) return arr;
  const pivot = arr[0];
  const left = [];
  const right = [];

  for (let i = 1; i < arr.length; i++) {
    if (arr[i] < pivot) left.push(arr[i]);
    else right.push(arr[i]);
  }

  // Создаёт новые массивы на КАЖДОМ уровне рекурсии
  return [...slowQuickSort(left), pivot, ...slowQuickSort(right)];
}`,
      explanation: 'Простой и читаемый код, но на каждом рекурсивном вызове создаются новые массивы left и right. При N=1000 это ~1000 дополнительных аллокаций. Пространственная сложность O(n log n) вместо O(log n), что нагружает Garbage Collector.'
    },
    commonMistakes: [
      'Выбор крайнего элемента как pivot без рандомизации — деградация до O(n²) на отсортированных или почти отсортированных данных.',
      'Неправильное базовое условие рекурсии: если написать left > right вместо left >= right — пропустим случай одного элемента.',
      'Создание новых массивов left/right на каждом уровне рекурсии вместо in-place partition — потеря главного преимущества quick sort.'
    ],
    checklist: [
      'Понять что делает partition — результат и инварианты до/после.',
      'Уметь трассировать partition на массиве из 4-5 элементов вручную.',
      'Объяснить худший случай и как его избежать.'
    ],
    interviewQA: [
      {
        question: 'Почему быстрая сортировка называется «быстрой», если её худшее время O(n²)? Как избежать худшего случая?',
        interviewerExpectation: 'Проверяется глубина понимания среднего случая, реального поведения алгоритма и практических защит.',
        verbalStructure: 'Объясните O(n log n) в среднем с малой константой, опишите худший случай (вырожденное деление), предложите рандомизацию pivot.',
        firstMention: 'Называется быстрой из-за малой константы в O(n log n) на практике и эффективной работы с кэшем процессора. Худшего O(n²) можно избежать рандомизацией pivot.',
        commonAnswerMistakes: 'Ответ что «у quicksort нет худшего случая O(n²)» или незнание как защититься.',
        strongAnswer: 'В среднем quicksort O(n log n) с меньшей константой чем у merge sort — работает in-place, эффективно использует кэш процессора (последовательный доступ к памяти в partition). Худший случай O(n²): pivot делит массив на 0 и N-1 элементов — это случается при выборе крайнего элемента на уже отсортированных данных. Защиты: (1) рандомизированный pivot: random(left, right); (2) медиана трёх: медиана первого, среднего, последнего элементов; (3) Introsort (в STL C++): при глубине рекурсии > 2*log N переключается на heap sort.',
        followUpQuestions: [
          'Какова пространственная сложность quick sort?',
          'Является ли быстрая сортировка стабильной?'
        ]
      }
    ],
    relatedTopics: ['complexity-basics', 'merge-sort'],
    quizzes: [
      {
        id: 'quiz-quick-1',
        question: 'Что определяет пространственную сложность in-place быстрой сортировки?',
        options: ['Вспомогательный массив копий', 'Глубина стека рекурсивных вызовов', 'Размер входного массива N', 'Она всегда O(1)'],
        correctAnswerIndex: 1,
        explanation: 'Алгоритм сортирует in-place (не создаёт копии), но каждый рекурсивный вызов добавляет фрейм в стек вызовов. В среднем глубина рекурсии O(log n), в худшем — O(n). Именно стек рекурсии определяет пространственную сложность.'
      }
    ],
    tasks: ['quick-sort-task']
  },
  {
    slug: 'merge-sort',
    title: 'Сортировка слиянием (Merge Sort)',
    section: 'advanced',
    sectionTitle: 'Дополнительные алгоритмы сортировки',
    order: 10,
    difficulty: 'Сложный',
    estimatedMinutes: 25,
    prerequisites: ['quick-sort'],
    summary: 'Изучение алгоритма сортировки слиянием. Рекурсивное деление пополам, механика слияния двух указателей и разбор почему гарантирован O(n log n).',
    whatToKnowBefore: 'Уверенное понимание рекурсии и того как строится дерево рекурсии.',
    keyTerms: [
      { term: 'Слияние (Merge)', definition: 'Ключевая операция: объединить два уже отсортированных массива в один отсортированный за время O(n). Два указателя сравнивают текущие элементы, меньший записывается в результат.' },
      { term: 'Рекурсивное дерево', definition: 'Визуальное представление рекурсивных вызовов. У merge sort дерево всегда сбалансированное: каждый уровень делит массив ровно пополам, глубина = log n.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Merge Sort гарантирует O(n log n) в абсолютно любом случае — в отличие от quick sort, который может деградировать. Это достигается за счёт строго сбалансированного деления: всегда пополам, независимо от данных.' },
      { type: 'heading', level: 2, content: 'Почему O(n log n) — разбор математики' },
      { type: 'code', language: 'javascript', content: `// Массив из 8 элементов. Рекурсивное дерево:
//
// Уровень 0 (1 вызов):  [8 элементов]
// Уровень 1 (2 вызова): [4 эл.] [4 эл.]
// Уровень 2 (4 вызова): [2][2] [2][2]
// Уровень 3 (8 вызовов):[1][1][1][1][1][1][1][1]  ← базовый случай
//
// Глубина = log₂(8) = 3 уровня
//
// На КАЖДОМ уровне суммарная работа по слиянию = O(n):
//   Уровень 3→2: 8 слияний по 1 эл. = 8 операций
//   Уровень 2→1: 4 слияния по 2 эл. = 8 операций
//   Уровень 1→0: 2 слияния по 4 эл. = 8 операций
//
// Итого: log(n) уровней × n операций на каждом = O(n log n)
// И это ГАРАНТИРОВАНО — деление всегда пополам!` },
      { type: 'heading', level: 2, content: 'Механика слияния двух массивов — разбор кода' },
      { type: 'code', language: 'javascript', content: `function merge(left, right) {
  const result = [];
  let i = 0; // указатель на текущую позицию в left
  let j = 0; // указатель на текущую позицию в right

  // Пока в обоих массивах есть элементы — сравниваем
  while (i < left.length && j < right.length) {
    if (left[i] <= right[j]) {
      //         ^^ Важно: <= а не < !
      // Это обеспечивает СТАБИЛЬНОСТЬ: при равных значениях
      // левый (из left) идёт первым, сохраняя исходный порядок.
      result.push(left[i]);
      i++;
    } else {
      result.push(right[j]);
      j++;
    }
  }

  // После выхода из while один из массивов исчерпан.
  // Дописываем оставшиеся элементы второго (они уже отсортированы!).
  // concat + slice работают за O(k) где k — оставшиеся элементы.
  return result.concat(left.slice(i)).concat(right.slice(j));
  //     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  //     Элегантная запись: если left[i..] пуст, left.slice(i) = [],
  //     concat([]) не добавляет ничего — работает без условий.
}` },
      { type: 'code', language: 'javascript', content: `function mergeSort(arr) {
  if (arr.length <= 1) return arr;
  // ^ Базовый случай: массив из 0 или 1 элемента уже «отсортирован»

  const mid = Math.floor(arr.length / 2);
  //          ^^^^^^^^^^^^^^^^^^^^^^^^^^^
  //  Math.floor гарантирует что left часть <= right части.
  //  Например: arr.length=5 → mid=2, left=[0..1], right=[2..4]

  const left = arr.slice(0, mid);  // создаём копии подмассивов
  const right = arr.slice(mid);    // (это и есть O(n) доп. память)

  // Рекурсивно сортируем обе части и сливаем результаты
  return merge(mergeSort(left), mergeSort(right));
}` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Слияние массивов [2, 8] и [1, 5] — шаг за шагом' },
      { type: 'list', content: [
        'i=0, j=0: left[0]=2 vs right[0]=1. 1 < 2 → result=[1], j=1.',
        'i=0, j=1: left[0]=2 vs right[1]=5. 2 < 5 → result=[1,2], i=1.',
        'i=1, j=1: left[1]=8 vs right[1]=5. 5 < 8 → result=[1,2,5], j=2.',
        'j=2 >= right.length → выходим из while.',
        'Дописываем остаток left начиная с i=1: left.slice(1) = [8].',
        'Итог: result.concat([8]) = [1, 2, 5, 8] ✓'
      ]}
    ],
    whenToUse: 'Когда гарантированно нужна O(n log n) без риска деградации. Когда важна стабильность. При сортировке больших файлов на диске (внешняя сортировка — merge sort идеален т.к. работает с последовательным доступом).',
    whenNotToUse: 'На устройствах с жёсткими ограничениями памяти — O(n) доп. памяти может быть критично.',
    rightExample: {
      title: 'Полная реализация сортировки слиянием',
      code: `function mergeSort(arr) {
  if (arr.length <= 1) return arr; // базовый случай

  const mid = Math.floor(arr.length / 2);
  const left = arr.slice(0, mid);
  const right = arr.slice(mid);

  return merge(mergeSort(left), mergeSort(right));
}

function merge(left, right) {
  const result = [];
  let i = 0, j = 0;

  while (i < left.length && j < right.length) {
    // <= для стабильности: равные из левой части идут первыми
    if (left[i] <= right[j]) {
      result.push(left[i++]);
    } else {
      result.push(right[j++]);
    }
  }

  // Дописываем остатки (один из массивов может быть непуст)
  return result.concat(left.slice(i)).concat(right.slice(j));
}

console.log(mergeSort([38, 27, 43, 3, 9, 82, 10]));
// [3, 9, 10, 27, 38, 43, 82]`,
      explanation: 'Ключевой момент: left.slice(i) и right.slice(j) в конце — берут остаток из того массива, который ещё не исчерпан. Поскольку оба массива уже отсортированы, просто дописываем всё что осталось.'
    },
    wrongExample: {
      title: 'Попытка in-place слияния без буфера — O(n²) по времени',
      code: `// ПСЕВДОКОД плохого подхода
function badMergeInPlace(arr, left, mid, right) {
  // Попытка слить два подмассива без дополнительной памяти.
  // Если при слиянии нужно вставить элемент из правой части
  // в середину левой — нужно сдвинуть все элементы левой части.
  // Это O(n) операций для каждого элемента правой части → O(n²) суммарно.
  // Весь смысл merge sort (O(n log n)) потерян.
}`,
      explanation: 'Корректная in-place сортировка слиянием с сохранением O(n log n) крайне сложна в реализации. Наивные попытки превращают слияние в O(n²). Используйте стандартный подход с буфером.'
    },
    commonMistakes: [
      'Использование < вместо <= в условии слияния: left[i] < right[j] вместо <=. Это ломает стабильность — при равных элементах из правой части они встанут перед левыми.',
      'Забыть про concat/дописывание остатков — если один массив исчерпан, но во втором ещё есть элементы, их нужно дописать целиком.',
      'Неверный расчёт mid: для нечётной длины Math.floor даёт правильный результат, Math.ceil тоже работает но даёт другие размеры частей.'
    ],
    checklist: [
      'Объяснить почему merge sort всегда O(n log n) (сбалансированное дерево рекурсии).',
      'Написать функцию merge с двумя указателями.',
      'Понять зачем нужен <= вместо < для стабильности.'
    ],
    interviewQA: [
      {
        question: 'Почему merge sort гарантирует O(n log n), а quick sort может деградировать до O(n²)?',
        interviewerExpectation: 'Проверяется понимание деревьев рекурсии и влияния сбалансированности разбиения.',
        verbalStructure: 'Объясните что merge sort делит строго пополам (log n уровней), а quick sort делит неравномерно в зависимости от pivot.',
        firstMention: 'Merge sort всегда делит массив строго пополам — глубина дерева всегда log n. Quick sort делит по pivot, который может быть крайним элементом.',
        commonAnswerMistakes: 'Ответ что «merge sort медленнее, поэтому стабильнее». Стабильность и скорость — разные свойства.',
        strongAnswer: 'В merge sort деление через Math.floor(length/2) всегда даёт два подмассива размером n/2. Дерево рекурсии строго сбалансировано: глубина = log n. На каждом уровне суммарное слияние = O(n). Итого: O(n log n) гарантировано для любых входных данных. В quick sort разбиение зависит от pivot. Если pivot каждый раз выбирается неудачно (крайний элемент отсортированного массива), одна часть пустая, другая N-1. Глубина дерева становится N → O(n²) операций.',
        followUpQuestions: [
          'Как merge sort используется для сортировки файлов, не помещающихся в RAM?',
          'Какова пространственная сложность merge sort?'
        ]
      }
    ],
    relatedTopics: ['quick-sort', 'heap-sort'],
    quizzes: [
      {
        id: 'quiz-merge-1',
        question: 'Почему в функции merge используется условие left[i] <= right[j], а не left[i] < right[j]?',
        options: [
          'Это не важно, результат одинаков',
          '<= обеспечивает стабильность: при равных значениях элемент из left идёт первым',
          'Чтобы избежать ошибки выхода за границы',
          'Для ускорения алгоритма'
        ],
        correctAnswerIndex: 1,
        explanation: 'При равных значениях <= оставляет элемент из left (оригинально левого подмассива) перед элементом из right. Это сохраняет исходный относительный порядок одинаковых элементов — определение стабильности сортировки.'
      }
    ],
    tasks: ['merge-sort-task']
  },
  {
    slug: 'heap-sort',
    title: 'Пирамидальная сортировка (Heap Sort)',
    section: 'advanced',
    sectionTitle: 'Дополнительные алгоритмы сортировки',
    order: 11,
    difficulty: 'Сложный',
    estimatedMinutes: 25,
    prerequisites: ['merge-sort'],
    summary: 'Изучение пирамидальной сортировки на основе двоичной кучи. Математика индексации дерева в массиве, механика heapify и in-place сортировка.',
    whatToKnowBefore: 'Понимание бинарных деревьев и их представления в массиве.',
    keyTerms: [
      { term: 'Двоичная куча (Binary Heap)', definition: 'Специальное дерево, хранимое в плоском массиве. В Max-Heap: каждый родитель больше своих детей. Максимум всегда в корне (индекс 0).' },
      { term: 'Heapify (Просеивание)', definition: 'Операция восстановления свойства кучи для узла: сравниваем узел с детьми, если нарушение — меняем с наибольшим ребёнком и рекурсивно опускаем ниже.' },
      { term: 'Математика индексов', definition: 'Для узла на индексе i: левый ребёнок = 2i+1, правый = 2i+2, родитель = floor((i-1)/2). Это позволяет хранить дерево без указателей — просто в массиве.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Пирамидальная сортировка решает проблему quick sort (нет гарантии O(n log n)) и merge sort (нужна доп. память): она гарантирует O(n log n) И работает in-place с O(1) доп. памяти. Цена — нестабильность и худшая работа с кэшем процессора.' },
      { type: 'heading', level: 2, content: 'Математика: как дерево лежит в массиве' },
      { type: 'code', language: 'javascript', content: `// Массив: [16, 14, 10, 8, 7, 9, 3, 2, 4, 1]
// Как дерево:
//         16          (индекс 0)
//       /    \\
//     14      10       (индексы 1, 2)
//    /  \\    /  \\
//   8    7  9    3     (индексы 3, 4, 5, 6)
//  / \\ /
// 2  4 1               (индексы 7, 8, 9)

// Формулы для узла на индексе i:
function leftChild(i)  { return 2 * i + 1; }  // 0→1, 1→3, 2→5
function rightChild(i) { return 2 * i + 2; }  // 0→2, 1→4, 2→6
function parent(i)     { return Math.floor((i - 1) / 2); } // 1→0, 3→1

// Проверка:
// Для i=1 (значение 14): left=3 (8), right=4 (7), parent=0 (16) ✓
// Для i=4 (значение 7):  left=9 (1), right=10 (нет), parent=1 (14) ✓` },
      { type: 'heading', level: 2, content: 'Как работает heapify — просеивание вниз' },
      { type: 'code', language: 'javascript', content: `function heapify(arr, size, i) {
  let largest = i;          // предполагаем что i — наибольший
  const left  = 2 * i + 1; // индекс левого ребёнка
  const right = 2 * i + 2; // индекс правого ребёнка

  // Проверяем левого ребёнка (если существует)
  if (left < size && arr[left] > arr[largest]) {
    largest = left;
  }
  // Проверяем правого ребёнка (если существует)
  if (right < size && arr[right] > arr[largest]) {
    largest = right;
  }

  // Если нарушение найдено — меняем и опускаемся ниже
  if (largest !== i) {
    [arr[i], arr[largest]] = [arr[largest], arr[i]];
    // Рекурсивно восстанавливаем в поддереве куда переместили элемент
    heapify(arr, size, largest);
    // ^ Важно передавать size, а не arr.length!
    //   size уменьшается во второй фазе сортировки —
    //   мы не трогаем уже отсортированный хвост.
  }
}` },
      { type: 'heading', level: 2, content: 'Алгоритм сортировки — два этапа' },
      { type: 'code', language: 'javascript', content: `function heapSort(arr) {
  const n = arr.length;

  // ЭТАП 1: Построение Max-Heap из произвольного массива
  // Начинаем с Math.floor(n/2) - 1, а не с n-1. Почему?
  // Индексы Math.floor(n/2) до n-1 — это ЛИСТЬЯ дерева.
  // Листья уже являются корректными кучами из 1 элемента.
  // Heapify нужно запускать только для ВНУТРЕННИХ узлов.
  for (let i = Math.floor(n / 2) - 1; i >= 0; i--) {
    heapify(arr, n, i);
  }

  // ЭТАП 2: Последовательно извлекаем максимум
  for (let i = n - 1; i > 0; i--) {
    // Корень (arr[0]) — текущий максимум.
    // Перемещаем его на финальную позицию (i).
    [arr[0], arr[i]] = [arr[i], arr[0]];

    // Восстанавливаем кучу для уменьшенного диапазона [0..i-1].
    // arr[i] уже на своём месте — не трогаем (размер = i).
    heapify(arr, i, 0);
  }
  return arr;
}` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Трассировка heapify для простого примера' },
      { type: 'list', content: [
        'Дано: arr = [1, 14, 10], size=3, i=0. Нарушение: 1 должен быть в корне, но он меньше детей.',
        'left = 2*0+1 = 1, arr[1]=14. right = 2*0+2 = 2, arr[2]=10.',
        'arr[left]=14 > arr[largest=0]=1 → largest = 1.',
        'arr[right]=10 > arr[largest=1]=14? Нет.',
        'largest=1 ≠ i=0 → swap arr[0] и arr[1] → arr = [14, 1, 10].',
        'Рекурсивно heapify(arr, 3, 1): left=3 >= size=3, right=4 >= size=3 — нет детей, нарушений нет.',
        'Результат: [14, 1, 10] — Max-Heap восстановлена ✓'
      ]}
    ],
    whenToUse: 'В системах реального времени где нужны гарантированные O(n log n) и O(1) памяти — встроенные системы, микроконтроллеры, ядра ОС.',
    whenNotToUse: 'В большинстве прикладных задач — Timsort и Quick Sort на практике быстрее из-за лучшей локальности кэша. Heap Sort нестабилен.',
    rightExample: {
      title: 'Полная реализация Heap Sort',
      code: `function heapSort(arr) {
  const n = arr.length;

  // Строим Max-Heap (только внутренние узлы, с конца к корню)
  for (let i = Math.floor(n / 2) - 1; i >= 0; i--) {
    heapify(arr, n, i);
  }

  // Извлекаем максимум по одному
  for (let i = n - 1; i > 0; i--) {
    [arr[0], arr[i]] = [arr[i], arr[0]]; // макс в конец
    heapify(arr, i, 0);  // восстанавливаем для уменьшенной кучи
  }
  return arr;
}

function heapify(arr, size, i) {
  let largest = i;
  const left = 2 * i + 1;
  const right = 2 * i + 2;

  if (left < size && arr[left] > arr[largest]) largest = left;
  if (right < size && arr[right] > arr[largest]) largest = right;

  if (largest !== i) {
    [arr[i], arr[largest]] = [arr[largest], arr[i]];
    heapify(arr, size, largest); // рекурсивно опускаем
  }
}`,
      explanation: 'heapify принимает size, а не arr.length — это позволяет уменьшать рабочую область во втором этапе, не трогая уже отсортированный хвост.'
    },
    wrongExample: {
      title: 'Создание реального дерева с узлами-объектами — убивает O(1) памяти',
      code: `// ОШИБОЧНЫЙ ПОДХОД
class HeapNode {
  constructor(value) {
    this.value = value;
    this.left = null;
    this.right = null;
  }
}

function heapSortWrong(arr) {
  // Создаём реальные объекты с указателями left/right
  // Это O(n) дополнительной памяти + накладные расходы GC
  // Теряем главное преимущество heap sort: O(1) пространственную сложность!
}`,
      explanation: 'Создание реальных узлов дерева в памяти с указателями уничтожает главное преимущество heap sort — O(1) доп. памяти. Используйте математическую индексацию (2i+1, 2i+2) в плоском массиве.'
    },
    commonMistakes: [
      'Запускать построение кучи с i = n-1, а не с Math.floor(n/2)-1 — листья уже корректные кучи, их не нужно обрабатывать (лишняя работа).',
      'Передавать arr.length вместо уменьшающегося i в heapify во втором этапе — алгоритм будет перемещать уже отсортированные элементы обратно.',
      'Перепутать 2i+1 и 2i+2 (левый vs правый ребёнок) — куча строится неверно, сортировка даст неверный результат.'
    ],
    checklist: [
      'Знать формулы: левый ребёнок = 2i+1, правый = 2i+2, родитель = floor((i-1)/2).',
      'Написать heapify с рекурсивным опусканием.',
      'Объяснить два этапа: построение кучи + последовательное извлечение максимума.'
    ],
    interviewQA: [
      {
        question: 'Почему heap sort гарантирует O(n log n) и O(1) памяти, но на практике медленнее quick sort?',
        interviewerExpectation: 'Проверяется понимание cache locality и физической работы с памятью.',
        verbalStructure: 'Подтвердите сложность, объясните Cache Miss в heap sort, сравните с последовательным доступом quick sort.',
        firstMention: 'Heap sort медленнее из-за плохой локальности кэша — обращения к элементам по формулам 2i+1 и 2i+2 прыгают по памяти хаотично.',
        commonAnswerMistakes: 'Ответ что «heap sort медленный потому что рекурсивный» — quick sort тоже рекурсивен.',
        strongAnswer: 'Heap sort обращается к элементам по формулам 2i+1 и 2i+2 — это прыжки по случайным адресам памяти. Процессор не может предсказать следующий адрес и вынужден читать данные из медленной RAM (Cache Miss). Quick sort при partition обходит массив последовательно двумя указателями — процессор предзагружает блоки памяти в кэш (Cache Hit, Spatial Locality). На практике это даёт quick sort преимущество в 2-3 раза, несмотря на идентичную асимптотику O(n log n).',
        followUpQuestions: [
          'Является ли Heap Sort стабильным алгоритмом?',
          'Что такое «куча» в контексте управления памятью JS — связана ли она с двоичной кучей?'
        ]
      }
    ],
    relatedTopics: ['merge-sort', 'exotic-sorts'],
    quizzes: [
      {
        id: 'quiz-heap-1',
        question: 'Где находится левый дочерний элемент для родителя на индексе i в массиве-куче?',
        options: ['i + 1', '2 * i', '2 * i + 1', '2 * i + 2'],
        correctAnswerIndex: 2,
        explanation: 'Формула: левый ребёнок = 2*i + 1. Правый ребёнок = 2*i + 2. Для корня (i=0): левый = 1, правый = 2 — что совпадает с визуальным представлением двоичного дерева.'
      }
    ],
    tasks: ['heap-sort-task']
  },
  {
    slug: 'exotic-sorts',
    title: 'Экзотические алгоритмы сортировки',
    section: 'advanced',
    sectionTitle: 'Дополнительные алгоритмы сортировки',
    order: 12,
    difficulty: 'Сложный',
    estimatedMinutes: 15,
    prerequisites: ['heap-sort'],
    summary: 'Концептуальный обзор Гномьей сортировки, сортировки Расческой и Коктейльной сортировки — с объяснением ключевых идей каждого алгоритма.',
    whatToKnowBefore: 'Понимание базовых принципов пузырьковой сортировки.',
    keyTerms: [
      { term: 'Гномья сортировка', definition: 'Алгоритм с одним циклом: «гном» движется вперёд если соседи в порядке, назад — если нет, меняя их местами. O(n²) в худшем, O(n) в лучшем.' },
      { term: 'Сортировка Расческой (Comb Sort)', definition: 'Улучшение пузырьковой: сравниваем не соседей, а элементы на расстоянии gap. Gap уменьшается на фактор 1.3 с каждым проходом. Устраняет «черепах».' },
      { term: 'Черепахи и зайцы', definition: 'В пузырьковой сортировке крупные элементы быстро «всплывают» (зайцы), а мелкие в конце сдвигаются на 1 позицию за проход (черепахи). Расческа и коктейльная — лечат эту проблему.' }
    ],
    theory: [
      { type: 'paragraph', content: 'Эти алгоритмы не применяются в продакшене, но показывают как небольшие изменения идеи пузырьковой сортировки могут существенно улучшить производительность — или сделать код минималистичным.' },
      { type: 'heading', level: 2, content: '1. Гномья сортировка — минимализм в коде' },
      { type: 'code', language: 'javascript', content: `function gnomeSort(arr) {
  let i = 0;
  while (i < arr.length) {
    // Если мы в начале ИЛИ соседи в правильном порядке — шаг вперёд
    if (i === 0 || arr[i] >= arr[i - 1]) {
      i++;
    } else {
      // Иначе — меняем местами и шаг назад для повторной проверки
      [arr[i], arr[i - 1]] = [arr[i - 1], arr[i]];
      i--;
    }
  }
  return arr;
}
// Весь алгоритм — 1 цикл, 1 условие. Минимум кода.
// O(n²) в худшем, O(n) на уже отсортированном.` },
      { type: 'heading', level: 2, content: '2. Сортировка Расческой — почему фактор именно 1.3?' },
      { type: 'paragraph', content: 'В пузырьковой сортировке «черепахи» (маленькие элементы в конце) двигаются в начало всего на 1 шаг за проход. Расческа устраняет это: начинает сравнивать элементы на большом расстоянии (gap), постепенно уменьшая его. Фактор 1.3 подобран эмпирически (experimentally) — он быстрее всего устраняет черепах.' },
      { type: 'code', language: 'javascript', content: `function combSort(arr) {
  let gap = arr.length;
  const shrink = 1.3; // эмпирически найденный оптимальный фактор
  let sorted = false;

  while (!sorted) {
    gap = Math.floor(gap / shrink);
    // gap уменьшается: n → n/1.3 → n/1.69 → ... → 1
    // При gap=1 это обычная пузырьковая сортировка

    if (gap <= 1) {
      gap = 1;
      sorted = true; // предполагаем что отсортировано, если нет обменов — выходим
    }

    for (let i = 0; i + gap < arr.length; i++) {
      if (arr[i] > arr[i + gap]) { // сравниваем через gap, а не соседей!
        [arr[i], arr[i + gap]] = [arr[i + gap], arr[i]];
        sorted = false; // был обмен — нужен ещё проход
      }
    }
  }
  return arr;
}
// В среднем O(n log n), в худшем O(n²/2^p) где p — количество инкрементов` },
      { type: 'heading', level: 2, content: '3. Коктейльная сортировка — пузырёк в обоих направлениях' },
      { type: 'paragraph', content: 'Стандартная пузырьковая идёт только слева направо — поэтому черепахи (мелкие элементы в конце) перемещаются медленно. Коктейльная чередует направления: сначала проход слева направо (максимум в конец), потом справа налево (минимум в начало). Это ускоряет сортировку для некоторых паттернов данных.' },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Коктейльная сортировка — логика прохода' },
      { type: 'list', content: [
        'Устанавливаем left=0, right=N-1 — границы неотсортированной части.',
        'Проход слева направо (как пузырьковая): максимум «всплывает» на right. Уменьшаем right на 1.',
        'Проход справа налево: минимум «тонет» на left. Увеличиваем left на 1.',
        'Повторяем пока left < right. Обе границы сжимаются с двух сторон.',
        'Сложность O(n²) в худшем, но на практике быстрее пузырьковой для массивов с «черепахами» в обоих концах.'
      ]}
    ],
    whenToUse: 'Изучайте для понимания как можно улучшать O(n²) алгоритмы (расческа — практическое улучшение пузырьковой) и для написания минималистичного кода (гномья).',
    whenNotToUse: 'Никогда не применяйте в промышленном JavaScript — встроенный Timsort лучше по всем метрикам.',
    rightExample: {
      title: 'Гномья сортировка — минимум кода',
      code: `function gnomeSort(arr) {
  let i = 0;
  while (i < arr.length) {
    if (i === 0 || arr[i] >= arr[i - 1]) {
      i++;
    } else {
      [arr[i], arr[i - 1]] = [arr[i - 1], arr[i]];
      i--;
    }
  }
  return arr;
}

console.log(gnomeSort([5, 3, 2, 4, 1])); // [1, 2, 3, 4, 5]`,
      explanation: 'Один цикл без вложенных. Гном движется вперёд если всё хорошо, назад с обменом — если нет. Простейший алгоритм сортировки по количеству строк кода.'
    },
    wrongExample: {
      title: 'Сортировка расческой с неверным фактором сжатия',
      code: `function badCombSort(arr) {
  let gap = arr.length;

  // Ошибка: фактор 2 вместо 1.3
  // При gap = N/2, N/4, N/8... финальные проходы с маленьким gap
  // не успевают устранить черепах.
  // Сложность падает обратно к O(n²).
  const shrink = 2; // Должно быть 1.3!

  while (gap > 1) {
    gap = Math.floor(gap / shrink);
    for (let i = 0; i + gap < arr.length; i++) {
      if (arr[i] > arr[i + gap]) {
        [arr[i], arr[i + gap]] = [arr[i + gap], arr[i]];
      }
    }
  }
  return arr;
}`,
      explanation: 'Фактор 1.3 — эмпирически оптимальный. Фактор 2 делает слишком мало промежуточных проходов, не успевая устранить «черепах». Результат: сортировка работает медленнее, чем могла бы.'
    },
    commonMistakes: [
      'Использовать фактор отличный от 1.3 в сортировке расческой — производительность ухудшается.',
      'Применять гномью сортировку для больших массивов — O(n²) неприемлемо при N > 1000.'
    ],
    checklist: [
      'Понимать идею гномьей сортировки: движение вперёд/назад по условию.',
      'Знать почему сортировка расческой быстрее пузырьковой (устраняет «черепах» большим начальным шагом).',
      'Объяснить проблему «черепах и зайцев» в пузырьковой сортировке.'
    ],
    interviewQA: [
      {
        question: 'Зачем нужна коктейльная сортировка, если её сложность всё равно O(n²)?',
        interviewerExpectation: 'Проверяется понимание «черепах» и «зайцев» в пузырьковой сортировке.',
        verbalStructure: 'Объясните проблему медленного движения мелких элементов («черепах»), покажите как двунаправленный проход решает это.',
        firstMention: 'Решает проблему «черепах» — мелких элементов в конце массива, которые в обычной пузырьковой двигаются в начало по 1 позиции за проход.',
        commonAnswerMistakes: 'Ответ что «коктейльная имеет O(n log n)» — нет, в худшем всё ещё O(n²).',
        strongAnswer: 'В стандартной пузырьковой сортировке крупные элементы («зайцы») быстро всплывают вправо за один проход. Но мелкие в правой части («черепахи») двигаются влево лишь на 1 позицию за проход — это очень медленно. Коктейльная ходит в обоих направлениях попеременно: проход слева направо поднимает максимум, проход справа налево опускает минимум. Это ускоряет перемещение «черепах». Однако в худшем случае всё равно O(n²) — лишь константа меньше.',
        followUpQuestions: [
          'Как сортировка расческой решает проблему черепах иначе?',
          'Почему для практического использования оба варианта хуже Timsort?'
        ]
      }
    ],
    relatedTopics: ['merge-sort', 'exotic-sorts'],
    quizzes: [
      {
        id: 'quiz-exotic-1',
        question: 'Какой оптимальный фактор сжатия (shrink factor) в сортировке расческой?',
        options: ['1.0', '1.3', '2.0', '3.14'],
        correctAnswerIndex: 1,
        explanation: 'Фактор 1.3 подобран эмпирически и даёт наилучшее уничтожение «черепах» за минимальное число проходов. Другие значения приводят к деградации производительности.'
      }
    ],
    tasks: []
  },
  {
    slug: 'production-practices',
    title: 'Сортировка в продакшене',
    section: 'production',
    sectionTitle: 'Сортировка в реальном JavaScript',
    order: 13,
    difficulty: 'Средний',
    estimatedMinutes: 20,
    prerequisites: ['stability-immutability'],
    summary: 'Реальные практики сортировки в коммерческих веб-приложениях: безопасные компараторы, клиент vs. сервер, обработка null/undefined, локали и мемоизация.',
    whatToKnowBefore: 'Уверенное понимание компараторов и иммутабельности.',
    keyTerms: [
      { term: 'localeCompare', definition: 'Метод строки для сравнения текстов с учётом языковых правил. Принимает локаль (\'ru\', \'en\') и опции (sensitivity: \'base\' — игнорировать регистр и диакритику).' },
      { term: 'useMemo', definition: 'React хук для мемоизации: пересчёт сортировки происходит только при изменении зависимостей (данных или критерия сортировки), а не при каждом рендере.' },
      { term: 'Серверная сортировка', definition: 'ORDER BY в SQL-запросе или индексы БД. Единственный правильный выбор при серверной пагинации — клиент видит только текущую страницу, а не весь набор данных.' }
    ],
    theory: [
      { type: 'paragraph', content: 'В реальной разработке сортировка пишется с нуля крайне редко. Основная сложность — правильные компараторы для грязных данных и выбор места сортировки: клиент или сервер.' },
      { type: 'heading', level: 2, content: 'Где сортировать: клиент или сервер?' },
      { type: 'code', language: 'javascript', content: `// КЛИЕНТСКАЯ сортировка — правильно когда:
// - Все данные уже загружены в браузер (< 500 записей)
// - Нет серверной пагинации
const sorted = [...data].sort(comparator);

// СЕРВЕРНАЯ сортировка — обязательно когда:
// - Есть пагинация (клиент видит только страницу из 20 записей из 10000)
// - Сортируем на клиенте только эти 20 — это бессмысленно!
// Правильно: передать параметр на сервер
const response = await fetch('/api/users?sort=name&order=asc&page=1');
// Сервер делает: SELECT * FROM users ORDER BY name ASC LIMIT 20 OFFSET 0` },
      { type: 'heading', level: 2, content: 'Защита от null и undefined в компараторе' },
      { type: 'code', language: 'javascript', content: `// Реальные данные часто содержат пропущенные поля:
const items = [
  { name: 'Яблоки', price: 100 },
  { name: null, price: 150 },    // null!
  { name: undefined, price: 80 }, // undefined!
  { name: '', price: 120 },       // пустая строка!
];

// БЕЗ ЗАЩИТЫ — крашится:
items.sort((a, b) => a.name.localeCompare(b.name));
// TypeError: Cannot read properties of null (reading 'localeCompare')

// С ЗАЩИТОЙ — отправляем пустые значения в конец:
items.sort((a, b) => {
  const aEmpty = a.name == null || a.name === ''; // == null ловит и null и undefined
  const bEmpty = b.name == null || b.name === '';

  if (aEmpty && bEmpty) return 0;  // оба пустые — порядок не важен
  if (aEmpty) return 1;            // a пустой → a идёт В КОНЕЦ
  if (bEmpty) return -1;           // b пустой → b идёт В КОНЕЦ

  return a.name.localeCompare(b.name, 'ru', { sensitivity: 'base' });
});` },
      { type: 'heading', level: 2, content: 'Мемоизация сортировки в React через useMemo' },
      { type: 'code', language: 'javascript', content: `import { useState, useMemo } from 'react';

function SortableTable({ data }) {
  const [sortKey, setSortKey] = useState('name');
  const [sortDir, setSortDir] = useState('asc'); // 'asc' | 'desc'

  // useMemo: сортировка пересчитывается ТОЛЬКО при изменении data, sortKey или sortDir
  // Без useMemo: сортировка запускается при КАЖДОМ рендере компонента
  const sortedData = useMemo(() => {
    return [...data].sort((a, b) => {
      const aVal = a[sortKey];
      const bVal = b[sortKey];

      // Защита от null
      if (aVal == null) return 1;
      if (bVal == null) return -1;

      // Числа — вычитаем, строки — localeCompare
      const cmp = typeof aVal === 'number'
        ? aVal - bVal
        : aVal.localeCompare(bVal, 'ru');

      return sortDir === 'asc' ? cmp : -cmp; // инвертируем для desc
    });
  }, [data, sortKey, sortDir]); // зависимости

  return (
    <table>
      <thead>
        <tr>
          <th onClick={() => setSortKey('name')}>Имя</th>
          <th onClick={() => setSortKey('age')}>Возраст</th>
        </tr>
      </thead>
      {/* рендер sortedData */}
    </table>
  );
}` },
    ],
    stepByStep: [
      { type: 'heading', level: 3, content: 'Логика обработки null/undefined в компараторе' },
      { type: 'list', content: [
        'Определите куда уходят пустые значения (обычно в самый конец — UX соображение).',
        'Проверяйте оба значения a и b ДО сравнения. Используйте == null (а не === null) — поймает и null и undefined одновременно.',
        'Если оба пустые — return 0 (их порядок не важен).',
        'Если a пустой — return 1 (a уходит вправо, то есть в конец).',
        'Если b пустой — return -1 (b уходит в конец).',
        'Только если оба не пустые — выполняем реальное сравнение через localeCompare или вычитание.'
      ]}
    ],
    whenToUse: 'Используйте localeCompare с явной локалью при сортировке текстовых полей для пользователя. Всегда копируйте массив перед sort() в React. Используйте useMemo для дорогих сортировок.',
    whenNotToUse: 'Не сортируйте на клиенте массивы из тысяч элементов — это блокирует Main Thread и «замораживает» UI. Используйте серверную сортировку или Web Workers.',
    rightExample: {
      title: 'Устойчивый продакшен-компаратор с обработкой пустых значений',
      code: `const items = [
  { name: 'Яблоки', price: 100 },
  { name: null, price: 150 },
  { name: 'Бананы', price: 80 }
];

items.sort((a, b) => {
  // null/undefined — в конец
  if (a.name == null) return 1;
  if (b.name == null) return -1;

  // Строки — с учётом русской локали, игнор. регистра
  return a.name.localeCompare(b.name, 'ru', { sensitivity: 'base' });
});

console.log(items.map(i => i.name));
// ['Бананы', 'Яблоки', null]`,
      explanation: 'Проверка == null (вместо === null) ловит и null и undefined одним условием. localeCompare с options гарантирует что «бананы» и «Бананы» считаются одинаковыми.'
    },
    wrongExample: {
      title: 'Падение приложения на грязных данных без проверок',
      code: `const items = [
  { name: 'Яблоки' },
  { name: null },
  { name: 'Бананы' }
];

// Краш без проверки null!
items.sort((a, b) => a.name.localeCompare(b.name));
// TypeError: Cannot read properties of null (reading 'localeCompare')
// Всё выполнение JS на странице остановится, пользователь увидит пустой экран`,
      explanation: 'null.localeCompare() вызывает TypeError и полностью останавливает выполнение JS на странице. В продакшене это означает «белый экран» для пользователя.'
    },
    commonMistakes: [
      'Вызывать localeCompare без предварительной проверки на null/undefined — гарантированный краш на реальных данных.',
      'Сортировать данные на клиенте при серверной пагинации — сортируется только текущая страница, пользователь думает что всё работает, но получает некорректный порядок.',
      'Не использовать useMemo для сортировки в React — при каждом рендере создаётся новый отсортированный массив даже если данные не изменились.'
    ],
    checklist: [
      'Уметь писать компараторы с защитой от null, undefined и пустых строк.',
      'Понимать когда нужна серверная сортировка (пагинация) vs. клиентская.',
      'Использовать localeCompare с локалью и опциями для русских строк.',
      'Оборачивать дорогие сортировки в useMemo с правильными зависимостями.'
    ],
    interviewQA: [
      {
        question: 'Как реализовать сортировку таблицы по клику на заголовок в React? Какие оптимизации применить?',
        interviewerExpectation: 'Проверяется понимание архитектуры React, иммутабельности, мемоизации и разделения ответственности.',
        verbalStructure: '1. Стейт: данные, ключ, направление. 2. useMemo с зависимостями. 3. toSorted() или spread. 4. Безопасный компаратор. 5. Серверная сортировка при больших данных.',
        firstMention: 'Хранить в state ключ колонки (sortKey) и направление (sortDir). Вычислять отсортированный массив через useMemo.',
        commonAnswerMistakes: 'Хранить отсортированный массив в state и обновлять его при каждом клике — теряем исходный порядок и не можем вернуться к нему.',
        strongAnswer: 'Правильная архитектура: (1) state = { data (исходный, не трогаем!), sortKey, sortDir }; (2) const sortedData = useMemo(() => [...data].sort(comparator), [data, sortKey, sortDir]) — пересчёт только при изменении зависимостей; (3) Компаратор с проверкой null, числа через вычитание, строки через localeCompare с локалью; (4) Клик по заголовку меняет sortKey и toggleit sortDir; (5) При N > 500 или пагинации — параметры сортировки передаём на сервер в query params, там SQL ORDER BY.',
        followUpQuestions: [
          'Что делать если сортировка 100 000 строк тормозит браузер?',
          'Как реализовать три состояния сортировки: ASC → DESC → сброс?'
        ]
      }
    ],
    relatedTopics: ['stability-immutability', 'sort-comparators'],
    quizzes: [
      {
        id: 'quiz-prod-1',
        question: 'В каком случае сортировку обязательно нужно перенести на сервер (в базу данных)?',
        options: [
          'Если на клиенте используется React',
          'Если в массиве есть значения null',
          'Если используется серверная пагинация (данные загружаются частями)',
          'Если нужно сортировать числа по убыванию'
        ],
        correctAnswerIndex: 2,
        explanation: 'При серверной пагинации клиент видит только 20 из 10 000 записей. Сортировка этих 20 даст правильный порядок только для текущей страницы. Чтобы получить корректный глобальный порядок — нужно ORDER BY на сервере, который имеет все данные.'
      }
    ],
    tasks: ['sort-strings-localeCompare']
  }
];
